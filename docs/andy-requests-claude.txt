4. Claude Session Viewer —— 在 Zed 裡用「網頁版的顯示品質」看／控制正在跑的 Claude Code session

================================================================================
0. 動機與目標
================================================================================

現況：Claude Code 跑在 tmux 裡，用 `--remote-control` 讓手機／網頁可以遠端控制。
Terminal 只能顯示純文字 + ANSI 顏色，沒有圖片、沒有精緻表格、tool call 無法折疊、
subagent 在做什麼完全看不到。但網頁與手機 app 顯示得很好。

目標：在 Zed 開一個 panel，接上「已經在跑的」那個 Claude session，用 Zed 既有的
markdown / diff / image render 能力顯示，並且能送輸入。tmux、手機、網頁、Zed 四邊
看到的是同一個 session。

非目標（明確排除）：
  - 不嵌 webview（GPUI 沒有 webview，gpui/gpui_macos 內 `WKWebView` 零命中）。
  - 不逆向 remote-control bridge 的網路協定。
  - 不取代 Zed 既有的 Agent Panel（那是 Zed 自己起的 ACP session，與本功能無關）。

--------------------------------------------------------------------------------
0.1 為什麼可行（已實測）
--------------------------------------------------------------------------------

Claude Code CLI 把完整結構化對話寫在本機檔案，不管輸入來自 terminal、手機還是網頁，
都是同一個 process 寫同一份檔。所以只要讀檔就能拿到 100% 的內容，包含 image block、
thinking block、結構化 tool result。

本文件所有欄位名稱都是在 2026-09-10 於 Claude Code 2.1.263 / 2.1.266 / 2.1.267 的
實機資料上驗證過的，不是推測。


================================================================================
1. 資料來源（實測）
================================================================================

--------------------------------------------------------------------------------
1.1 Session 註冊表 —— ~/.claude/sessions/<pid>.json
--------------------------------------------------------------------------------

每個活著的 claude process 一個檔。這是列舉與追蹤的唯一權威來源。

實例（原文照抄）：

    {
      "pid": 10064,
      "sessionId": "4e2e3600-89c0-4cd5-9994-525c708559ab",
      "cwd": "/Users/andy/go/src/github.com/poi5305/zed",
      "startedAt": 1789007244364,
      "procStart": "Thu Sep 10 02:27:23 2026",
      "version": "2.1.267",
      "peerProtocol": 1,
      "peerFeatures": ["notify_idle", "reply_across_default_dirs", "artifact_yield"],
      "kind": "interactive",
      "entrypoint": "cli",
      "pidDomain": "darwin",
      "tmux": "zed:@6.%8",
      "messagingSocketPath": "/tmp/cc-socks/10064.sock",
      "name": "zed-e4",
      "nameSource": "derived",
      "nameSince": 1789007244364,
      "status": "busy",
      "updatedAt": 1789009278063,
      "statusUpdatedAt": 1789009278063,
      "bridgeSessionId": "session_01Tf2BzmxZDH3YKYpdxtSrpD"
    }

我們用到的欄位：
  sessionId            → transcript 檔名。**權威來源，不是檔案系統掃描。**
  cwd                  → 用來過濾「跟目前 Zed project 相關」的 session。
  tmux                 → "<session_name>:<window_id>.<pane_id>"，直接可當 send-keys target。
                         **可能不存在**（非 tmux 內啟動的 session，例如被 spawn 的子 session）。
  pid + procStart      → 判活。兩者都要比對，procStart 用來防 pid 重用。
  status / statusUpdatedAt → "busy" / 其他；做 UI 上的忙碌指示。
  updatedAt            → heartbeat；太舊視為 stale。
  version              → 顯示用，也用來對未知格式做降級。
  kind                 → "interactive"；非 interactive 的不列。
  name                 → 顯示名稱（例：zed-e4）。
  messagingSocketPath  → 見 §6.2，第一版不使用。
  bridgeSessionId      → 有值代表這個 session 正被 remote-control 接管；UI 上標一個 icon。

旁邊還有 `~/.claude/sessions/<pid>.<sha256>.key`（108 bytes）。
**視為機密，不得讀取、不得記錄、不得傳輸。** 與 messagingSocketPath 成對，
是 §6.2 那條未採用的通道的憑證。

註冊檔是 stale 的（實機上看到好幾天前的殘留），一定要判活。

--------------------------------------------------------------------------------
1.2 主 transcript —— ~/.claude/projects/<slug>/<sessionId>.jsonl
--------------------------------------------------------------------------------

JSON Lines，append-only。

**不要自己算 <slug>。** 看起來是把 cwd 的 `/` 換成 `-`，但這是未文件化的規則。
一律用 glob 定位：

    ~/.claude/projects/*/<sessionId>.jsonl

已知的 top-level `type` 值（實測列舉，非窮舉）：

    user                對話中的 user 訊息，或 tool_result 回填
    assistant           assistant 訊息
    attachment          系統注入的上下文（見 §1.5）
    system              系統事件，用 `subtype` 再分（見下）
    file-history-snapshot
    mode / permission-mode / bridge-session / atis-latch
    last-prompt / ai-title / queue-operation

已知的 system `subtype`：

    compact_boundary    壓縮分界（見 §3.2）—— 必須處理
    bridge_status       remote-control 狀態變化
    local_command       本機 slash command 的 stdout（含 ANSI escape）
    away_summary / turn_duration

`message.content` 內的 block type：`text` / `thinking` / `tool_use` / `tool_result` / `image`。

每筆記錄的通用欄位：

    uuid                本筆 id
    parentUuid          前一筆的 uuid，**null 代表這是一個 root**
    isSidechain         true 代表這筆屬於 subagent（在主檔內為 false；見 §4）
    timestamp / sessionId / cwd / gitBranch / version
    toolUseResult       tool 的**結構化**結果（例：{stdout: ...}）。
                        比 content block 裡的文字更豐富，rich render 主要靠這個。
    isMeta / userType / requestId / promptId / effort / perTurnEffort

--------------------------------------------------------------------------------
1.3 Subagent transcript —— <sessionId>/subagents/agent-<agentId>.{jsonl,meta.json}
--------------------------------------------------------------------------------

meta.json（原文照抄）：

    {
      "agentType": "general-purpose",
      "description": "WP1 forward ports WSL/Docker",
      "toolUseId": "toolu_01Qv7iieSWkvLp17oyUTbodG",
      "spawnDepth": 1,
      "requestShape": "background",
      "requestNonInteractive": true,
      "model": "opus"
    }

  toolUseId   → 對應主 transcript 裡那個 `tool_use` block 的 `id`。
                **這是 subagent 掛回主對話正確位置的唯一連結。**
  spawnDepth  → 巢狀層數，支援 subagent 再開 subagent。
  agentType / description / model / requestShape → 卡片上的顯示資訊。

agent-<agentId>.jsonl 與主檔同格式，差別：
  - 每筆都有 `isSidechain: true` 與 `agentId`
  - 自己的 parentUuid 鏈，root 的 parentUuid 為 null
  - 另有 `attributionAgent`

**這些檔在 subagent 執行期間就持續在寫**（實測：跑到一半的 agent 檔已 689KB 且 mtime
持續更新）。所以「即時看 agent 在做什麼」不需要任何額外機制，tail 這個檔就是了。

完成訊號：主 transcript 出現對應 `toolUseId` 的 `tool_result`。

--------------------------------------------------------------------------------
1.4 卸載的大型輸出 —— <sessionId>/tool-results/
--------------------------------------------------------------------------------

超過門檻的 tool 輸出不會寫進 transcript，而是存成獨立檔，transcript 裡放：

    <persisted-output>
    Output too large (30.2KB). Full output saved to: <絕對路徑>
    Preview (first 2KB):
    ...

檔名兩種：`<9碼隨機>.txt`（tool 輸出）、`hook-<uuid>-stdout.txt`（hook 輸出）。

**這是 Zed 版明顯勝過 terminal 的地方**：terminal 只看得到 2KB preview，
Zed 可以在展開時 lazy load 整個檔。要 parse 那個 marker 把路徑抓出來。

--------------------------------------------------------------------------------
1.5 Attachment
--------------------------------------------------------------------------------

`type: "attachment"` 的記錄帶 `attachment.type`，實測值：

    environment / model / deferred_tools_delta / mcp_instructions_delta
    hook_success / invoked_skills / prompt_snapshot / queued_command
    compact_file_reference / edited_text_file / file / opened_file_in_ide
    selected_lines_in_ide / total_tokens_reminder / agent_listing_delta

多數是雜訊，預設全部收在一個可展開的「context」摺疊區，不要平鋪在對話流裡。


================================================================================
2. Session 探索與列舉
================================================================================

流程：

  1. 列 `~/.claude/sessions/*.json`，逐檔 parse（單檔壞掉就跳過該檔，不要整批失敗）。
  2. 判活：pid 存在 **且** `procStart` 相符 **且** `updatedAt` 在合理範圍內。
  3. 過濾 `kind == "interactive"`。
  4. 用 glob `~/.claude/projects/*/<sessionId>.jsonl` 定位 transcript；找不到就標成
     "no transcript" 而不是丟掉（session 可能剛啟動）。
  5. 依 `cwd` 是否落在目前 Zed project 的 worktree 內，分成「本專案」與「其他」兩組。

**三個實測出來的坑，一定要照做：**

  (a) 不要用 tmux 掃描來偵測 claude。`pane_current_command` 實測顯示的是版本號
      （`2.1.267`），因為 argv[0] 指到 `~/.local/share/claude/versions/<ver>`，
      不是 `claude`。反過來要從註冊表的 `tmux` 欄位去對應 pane。

  (b) 不要讀別的 process 的環境變數。macOS 下 `ps eww -p <其他 pid>` 回空
      （SIP／hardened runtime）；Linux 的 `/proc/<pid>/environ` 才可行。
      不要寫成只在 Linux 能動的邏輯。

  (c) 不要自己組 project slug 路徑，用 glob。


================================================================================
3. Transcript 模型 —— 這一節做錯會從根上歪
================================================================================

--------------------------------------------------------------------------------
3.1 它是一棵樹，不是平坦 log
--------------------------------------------------------------------------------

每筆有 `uuid` + `parentUuid`。檔案是 append-only，但**對話內容是從 leaf 沿
parentUuid 往回走的那條路徑**，不是檔案的行序。

rewind（Esc Esc）、`--resume` 分支、compact 都會在同一個檔裡留下已被拋棄的分支。
線性播放會把死掉的分支畫出來。

正確做法：
  - 維護 `uuid -> record` 的 map，以及目前的 leaf。
  - 增量 tail 拿到新 record → 塞進 map、更新 leaf、重算 path。
  - 正常情況 path 只在尾端 append，diff 極小；只有 rewind 才整段換掉。

--------------------------------------------------------------------------------
3.2 /compact —— 實測結論（在 33 筆歷史 compact 上驗證）
--------------------------------------------------------------------------------

**compact 不換檔、不換 sessionId。**（掃過全部 294 個歷史 transcript，
sessionId 與檔名不符的案例：0。）

它在同一個檔裡寫入一筆：

    {
      "type": "system",
      "subtype": "compact_boundary",
      "parentUuid": null,                                   ← 鏈條被刻意切斷
      "logicalParentUuid": "0be38277-...",                  ← 指回壓縮前的 leaf
      "content": "Conversation compacted",
      "level": "info",
      "compactMetadata": {
        "trigger": "manual",
        "preTokens": 792090,
        "postTokens": 16995,
        "cumulativeDroppedTokens": 775095,
        "durationMs": 133856,
        "preservedSegment": { "headUuid": "...", "anchorUuid": "...", "tailUuid": "..." },
        "preservedMessages": { "anchorUuid": "...", "uuids": [...], "allUuids": [...] }
      }
    }

緊接著是一筆 `user` 記錄，帶 `isCompactSummary: true`，內容是
"This session is being continued from a previous conversation..." 的摘要。

推論出的規則：
  - N 次 compact → 檔案裡有 N+1 個 root（實測：boundaries=5 的檔有 6 個 root）。
  - 從 leaf 往回走會停在最近一個 compact_boundary，這正是「模型現在看得到的
    上下文」。
  - 要顯示完整歷史，就在 boundary 處改跟 `logicalParentUuid` 繼續往回走。

UI 要求：
  - 在 boundary 處畫一條明確的分界，顯示 `trigger` 與 `preTokens -> postTokens`。
  - 分界以上的歷史**預設折疊**，可展開（這是 terminal 做不到、網頁版也不見得有的優勢）。
  - `isCompactSummary: true` 那筆要標成「摘要」樣式，不要當成使用者真的打了這段字。
  - `trigger` 實測只出現過 `"manual"`；對其他值（很可能有 `"auto"`）要容錯，
    未知值就原樣顯示。

--------------------------------------------------------------------------------
3.3 /clear 與 resume
--------------------------------------------------------------------------------

`/clear` 換 sessionId → 換一個新的 transcript 檔，pid 不變。

所以：**主要監看對象是 `~/.claude/sessions/<pid>.json`，不是 transcript 檔。**
sessionId 一變就切檔、清畫面、重建 map。只 watch transcript 的話，clear 之後
Zed 會永遠停在舊對話。

舊檔不會被截斷或改寫，所以 byte offset 不會錯亂。但仍要做 sanity check：
檔案 size < 已記錄的 offset → 從頭重讀。


================================================================================
4. Subagent / Workflow 檢視
================================================================================

這是本功能相對網頁版與 terminal 的**主要價值**：同時看多個 agent 在做什麼。

--------------------------------------------------------------------------------
4.1 資料串接
--------------------------------------------------------------------------------

  1. 主 transcript 出現 `tool_use`，name 為 `Agent`（或其他會 spawn 的 tool）。
  2. 掃 `<sessionId>/subagents/*.meta.json`，用 `toolUseId` 對回那個 block 的 `id`。
  3. 對上了就在主對話的那個位置掛一張可展開的 agent 卡片。
  4. 展開 → tail `agent-<agentId>.jsonl`，用跟主 transcript 完全相同的 render pipeline。
  5. `spawnDepth > 1` → 巢狀顯示。

狀態判定：
  running  = 主 transcript 尚無對應 toolUseId 的 tool_result
  done     = tool_result 已出現
  進度提示  = agent jsonl 的最後一筆 record（最近一次 tool_use 的名稱／目標）+ mtime

主 transcript 另有 `pendingBackgroundAgentCount` 欄位，可直接當 header 上的計數徽章。

--------------------------------------------------------------------------------
4.2 UI：agent 切換
--------------------------------------------------------------------------------

Panel 頂端一列 tab／chip：

    [ Main ]  [ WP1 forward ports WSL/Docker ●]  [ WP3 project_manager fixes ●]  ...

  - 標籤文字用 meta 的 `description`；hover 顯示 `agentType` / `model` / `spawnDepth`。
  - `●` 表示還在跑（用 mtime + 有無 tool_result 判定）。
  - 點一下把主檢視切到那個 agent 的 transcript；Main 切回主對話。
  - 主對話裡對應位置的 agent 卡片也可以就地展開（inline），兩種入口都要有。
  - agent 已結束的仍要留在列上（灰掉），才能事後回看。
  - 這是**唯讀**檢視。不提供對 subagent 直接輸入。

Workflow 的 `agent()` 產出同樣落在 `subagents/`（meta 帶 `model` 欄位），
所以不需要為 Workflow 另做一套；同一個機制涵蓋。


================================================================================
5. UI 與 render
================================================================================

  - 落點：新的 dock panel，或 workspace item（tab）。**不要動 Zed 既有的 Agent Panel。**
  - 對話流用 Zed 既有的 markdown render；`ContentBlock::Image` 在
    crates/acp_thread/src/acp_thread.rs:1746 已經支援圖片，沿用同一條路徑。
  - `thinking` block：預設折疊。
  - `tool_use` / `tool_result`：折疊卡片。優先用 top-level `toolUseResult`（結構化）
    而不是文字 content block 來做 rich render（diff、檔案內容、命令輸出）。
  - `<persisted-output>` marker：顯示 preview，加一個「載入完整輸出」按鈕（§1.4）。
  - attachment：全部收進一個折疊的 context 區。
  - `local_command` 的 content 含 ANSI escape，要先 strip 或轉成樣式。
  - Header：session name、cwd、version、status、bridge 圖示、tmux target、pending agent 數。
  - 未知的 record type / block type / attachment type → **降級成 raw JSON 摺疊區顯示，
    絕對不要丟棄、不要 panic**。格式是私有的，一定會變。


================================================================================
6. 輸入通道
================================================================================

--------------------------------------------------------------------------------
6.1 第一版：tmux send-keys
--------------------------------------------------------------------------------

target 直接來自註冊表的 `tmux` 欄位（例：`zed:@6.%8`），不需要自己找 pane。

要處理：
  - 多行輸入的送法（bracketed paste）。
  - Esc 中斷。
  - 註冊表沒有 `tmux` 欄位的 session → 輸入框停用，只提供唯讀檢視。

--------------------------------------------------------------------------------
6.2 未採用：本機 IPC socket
--------------------------------------------------------------------------------

`messagingSocketPath` (`/tmp/cc-socks/<pid>.sock`) 加上 `~/.claude/sessions/<pid>.<sha>.key`
看起來是一條乾淨的本機 IPC。完全未文件化，協定不明。

**第一版不碰。** 記錄在此供日後評估。key 檔內容任何情況下都不得寫入 log。


================================================================================
7. Remote 支援
================================================================================

架構上沒有新東西：全部是「讀遠端檔案 + 遠端 tmux」。沿用既有的
crates/proto/proto/tmux.proto 那條路。

需要新增：

  - RPC：列出遠端的 claude sessions。在 remote_server 上讀 `~/.claude/sessions/*.json`、
    判活、glob transcript 路徑，回傳**結構化結果**。
    不要把 raw JSON 丟給 client 解 —— 遠端的 Claude Code 版本可能與本機不同，
    格式的相容性處理要留在產生端。

  - RPC：tail transcript。輸入 `(path, offset)`，回傳新增的 bytes 與新的 offset。
    同一支 RPC 也要能 tail subagent jsonl 與讀 tool-results 檔。

  - **第一版用輪詢（約 1 秒），不要做 remote file watch。**
    這些檔在 worktree 之外，走不到既有的 worktree scanner；而輪詢一個 append-only
    檔的成本可以忽略，省掉一整套遠端 watch 的生命週期問題。

  - `~` 必須在遠端 resolve，不能用本機 home。

Client 端抽一個 source trait（list / tail / read_blob 三個方法），
local 直接讀檔、remote 走 proto，UI 層完全不分本地遠端。


================================================================================
8. 風險
================================================================================

  - 這些檔案格式全是私有且未文件化的，會隨版本變。
    緩解：唯讀為主；未知欄位一律降級顯示；註冊表的 `version` 欄位可用來做提示。
    最壞情況是某段 render 不漂亮，不會弄壞任何 session。

  - `.key` 檔與 `messagingSocketPath` 屬敏感資訊，不讀、不記、不傳。

  - transcript 可能很大（實測單檔 2.7MB / 2695 筆），且 subagent 檔在跑的時候
    分分鐘幾百 KB。要做增量 parse 與虛擬捲動，不要整檔重 parse 重 render。

  - 註冊表有 stale 殘留，判活不能只看檔案存在。


================================================================================
9. 分期
================================================================================

  P1  本機唯讀：session 列舉 + 主 transcript rich render + compact/clear 正確處理。
      這一段本身就有價值，且完全無副作用。

  P2  Subagent / workflow 檢視與切換（§4）。

  P3  輸入（tmux send-keys）。

  P4  Remote（§7）。

P1 有兩條必須一開始就做對，其餘都能事後補：
  (1) 跟註冊表 `<pid>.json` 的 sessionId 走，不跟 transcript 檔名走。
  (2) 從 leaf 沿 parentUuid 走，遇 compact_boundary 用 logicalParentUuid 續接，
      不做線性播放。


================================================================================
================================================================================
附錄 A：P1 任務書（本機唯讀）
================================================================================
================================================================================

P1 範圍：本機、唯讀、只有主 transcript。
明確排除：subagent 檢視（P2）、輸入（P3）、remote（P4）。
但 WP2 的資料結構必須從一開始就能容納 `isSidechain` 記錄而不崩潰。

新 crate：`crates/claude_sessions/`
檔案配置（`[lib] path = "src/claude_sessions.rs"`，不使用 mod.rs）：

    src/claude_sessions.rs          crate root、actions、init            (WP0)
    src/session_registry.rs         純函式：註冊表解析與判活              (WP1)
    src/transcript.rs               純函式：記錄解析與對話樹              (WP2)
    src/session_store.rs            非同步：探索、tail、生命週期          (WP3)
    src/claude_sessions_panel.rs    Panel UI                             (WP4)
    src/claude_sessions_button.rs   status bar 按鈕                      (WP0)

--------------------------------------------------------------------------------
A.0 共通規則（每一份派工 prompt 都要帶）
--------------------------------------------------------------------------------

  - 逐字附上 codegraph 指示段落（見 A.7 派工樣板）。
  - 禁止 `git stash` / `git checkout` / `git restore` / `git reset`。
  - 「發現規格自相矛盾、或你正在挑輸入／加資料讓某個測試過，停下來回報，
    不要換輸入。回報『做不到』是成功的結果。」
  - 遵守 crates/../CLAUDE.md 的 Rust 規則：不用 `unwrap()`，錯誤用 `?` 傳遞，
    不用 `let _ =` 吞錯，變數用完整單字，註解只解釋「為什麼」。
  - 最小差異：不順手格式化、不改既有 public API。

**effort 的派法（2026-09-10 grok 額度用盡後的調整）**

  `Agent` tool 只能換 model，不能指定 effort。凡是本任務書寫「Opus 5 high」或
  「Opus 5 medium」的工作包，都要包成單隻 `Workflow`，用 `agent()` 的 effort 參數指定，
  不能直接丟 `Agent` tool（那樣 effort 是預設值，等於沒照表派）。
  寫「agy `gemini-3.8-flash-*`」的則走 CLI，effort 在 `--model` 字串裡。


================================================================================
A.1 WP0 —— crate 骨架與 wiring
================================================================================

模型：agy `gemini-3.8-flash-high`（機械性接線）
相依：無。最先做。

**事實清單**

抄 `crates/tmux_sessions/` 的結構，逐項對應：

  1. `Cargo.toml`（repo root）三處各加一行，維持字母序：
       - workspace members  ：參考 Cargo.toml:224 `"crates/tmux_sessions",`
       - workspace deps     ：參考 Cargo.toml:493 `tmux_sessions = { path = "crates/tmux_sessions" }`
  2. `crates/zed/Cargo.toml`：參考 :223 `tmux_sessions.workspace = true`
  3. `crates/zed/src/main.rs:745` 附近加 `claude_sessions::init(cx);`
  4. `crates/zed/src/zed.rs`：
       - :604  `let claude_sessions_button = cx.new(|_| claude_sessions::ClaudeSessionsButton::new());`
       - :640  `status_bar.add_left_item(claude_sessions_button, window, cx);`
       - :790  `let claude_sessions_panel = claude_sessions::ClaudeSessionsPanel::load(workspace_handle.clone(), cx.clone());`
       - :817  `add_panel_when_ready(claude_sessions_panel, workspace_handle.clone(), cx.clone()),`
  5. `crates/claude_sessions/Cargo.toml` 抄 `crates/tmux_sessions/Cargo.toml`，
     `[lib] path = "src/claude_sessions.rs"`，`doctest = false`。
     額外相依：`serde`、`serde_json`、`fs`、`paths`、`markdown`、`language`、`theme`、`settings`。
  6. `crates/claude_sessions/LICENSE-GPL` 比照其他新 crate（symlink 或複製，與
     `crates/tmux_sessions/LICENSE-GPL` 一致）。
  7. crate root 抄 `crates/tmux_sessions/src/tmux_sessions.rs:1-26` 的形狀：
     `actions!(claude_sessions, [ToggleFocus])` + `pub fn init(cx: &mut App)`
     內用 `cx.observe_new` 註冊 `toggle_panel_focus::<ClaudeSessionsPanel>`。
  8. `claude_sessions_button.rs` 抄 `crates/tmux_sessions/src/tmux_sessions_button.rs`
     全檔（57 行），只換名稱、`IconName` 與 aria label。
     圖示用既有 `IconName` 的既有值，**不得新增資產**。

WP0 階段 panel 可以是空殼（`Render` 回一個佔位 div），只要能編譯、能開關。

**驗收**：`./script/clippy` 通過；Zed 啟動後 status bar 出現按鈕、可開關 panel。


================================================================================
A.2 WP1 —— session_registry.rs（純函式）
================================================================================

模型：agy `gemini-3.8-flash-high`
理由：規格可以寫死到欄位層級，錯了 typecheck 與測試會紅。
相依：WP0。

**事實清單**

輸入：`~/.claude/sessions/<pid>.json` 的檔案內容字串。實測樣本（原文照抄）：

    {"pid":10064,"sessionId":"4e2e3600-89c0-4cd5-9994-525c708559ab",
     "cwd":"/Users/andy/go/src/github.com/poi5305/zed","startedAt":1789007244364,
     "procStart":"Thu Sep 10 02:27:23 2026","version":"2.1.267","peerProtocol":1,
     "peerFeatures":["notify_idle","reply_across_default_dirs","artifact_yield"],
     "kind":"interactive","entrypoint":"cli","pidDomain":"darwin",
     "tmux":"zed:@6.%8","messagingSocketPath":"/tmp/cc-socks/10064.sock",
     "name":"zed-e4","nameSource":"derived","nameSince":1789007244364,
     "status":"busy","updatedAt":1789009278063,"statusUpdatedAt":1789009278063,
     "bridgeSessionId":"session_01Tf2BzmxZDH3YKYpdxtSrpD"}

第二個實測樣本**沒有 `tmux` 欄位**（子 session）：

    {"pid":17694,"sessionId":"095bcff6-b9a8-4584-a3c6-861f16c9a807",
     "cwd":"/Users/andy/go/src/github.com/poi5305/zed","startedAt":1789008783382,
     "procStart":"Thu Sep 10 02:53:02 2026","version":"2.1.267","peerProtocol":1,
     "peerFeatures":[...],"kind":"interactive","entrypoint":"cli","pidDomain":"darwin",
     "messagingSocketPath":"/tmp/cc-socks/17694.sock","name":"zed-28",
     "nameSource":"derived","nameSince":1789008783382,"status":"busy",
     "updatedAt":1789009345355,"statusUpdatedAt":1789009345355,
     "bridgeSessionId":"session_01DzrBq3KnHvrXgSL43UVZgB"}

要交付的東西：

  pub struct RegisteredSession {
      pub process_id: u32,
      pub session_id: String,
      pub working_directory: PathBuf,
      pub process_start: String,          // 原樣保留，只做字串相等比對
      pub version: String,
      pub kind: String,
      pub name: Option<String>,
      pub status: Option<String>,
      pub updated_at: Option<i64>,        // epoch millis
      pub tmux_target: Option<String>,    // 保留給 P3，P1 只顯示
      pub bridge_session_id: Option<String>,
  }

  /// 解析單一註冊檔。未知欄位忽略，缺少必要欄位回 Err。
  pub fn parse_registered_session(contents: &str) -> anyhow::Result<RegisteredSession>

  pub enum Liveness { Live, StaleHeartbeat, ProcessGone, PidReused }

  /// 判活。process_start_of_pid 是注入的查詢函式，讓這支可以被測。
  /// 回傳 Live 的條件三者皆須成立：
  ///   1. process_start_of_pid(pid) 回 Some
  ///   2. 該值與 session.process_start 字串相等（不相等 → PidReused）
  ///   3. now_millis - updated_at <= stale_after_millis（超過 → StaleHeartbeat）
  ///      updated_at 為 None 時視為不 stale（只靠 pid 判定）
  pub fn liveness(
      session: &RegisteredSession,
      now_millis: i64,
      stale_after_millis: i64,
      process_start_of_pid: &dyn Fn(u32) -> Option<String>,
  ) -> Liveness

  /// 只保留 kind == "interactive" 且 Liveness::Live 的項目，
  /// 依 (working_directory 是否等於或位於 project_root 之下, name) 排序，
  /// 本專案的排前面。
  pub fn visible_sessions(...) -> Vec<RegisteredSession>

**硬性要求**

  - `serde` 反序列化必須忽略未知欄位（不要 `deny_unknown_fields`）。
    格式是私有的，新增欄位不得造成失敗。
  - 單一檔案解析失敗不得影響其他檔案 —— 呼叫端逐檔處理，這支只負責單檔。
  - 不得在此模組做任何檔案 I/O、不得取得系統時間、不得查 process。
    全部由參數注入。這是為了讓它 100% 可測。
  - **不得讀取、記錄或回傳 `~/.claude/sessions/<pid>.<sha>.key` 的任何內容。**
    也不得把 `messagingSocketPath` 放進 `RegisteredSession`（P1 用不到，
    少一個洩漏面）。

**回歸測試（住在套件裡，寫測試的人自己跑）**

  1. 完整樣本解析成功，每個欄位值逐一斷言。
  2. 缺 `tmux` 的樣本解析成功，`tmux_target` 為 None。
  3. 樣本加一個沒見過的欄位 `"futureField": 123` 仍解析成功。
  4. 缺 `sessionId` → Err。
  5. liveness：pid 不存在 → ProcessGone。
  6. liveness：pid 存在但 process_start 不同 → PidReused。
  7. liveness：pid 與 process_start 都對、updated_at 超過門檻 → StaleHeartbeat。
  8. liveness：三條件都滿足 → Live。
  9. visible_sessions 過濾掉 `kind != "interactive"`，並把本專案的排在前面。


================================================================================
A.3 WP2 —— transcript.rs（純函式）★ 單點失效處
================================================================================

模型：Opus 5 high（grok 額度已用盡；xhigh 不用於 code 生成）
理由：跨概念（樹重建 + compact 語意 + 增量），沒有現成測試守著，做錯整個功能歪掉。
相依：WP0。可與 WP1 並行。

**事實清單 —— 記錄格式**

JSON Lines，append-only。每筆的通用欄位：

    uuid              String（部分 type 沒有，例如 mode / last-prompt / ai-title）
    parentUuid        String | null   ← null 代表 root
    isSidechain       bool
    type              "user" | "assistant" | "attachment" | "system"
                      | "file-history-snapshot" | "mode" | "permission-mode"
                      | "bridge-session" | "atis-latch" | "last-prompt"
                      | "ai-title" | "queue-operation"
    subtype           僅 type=="system" 時有意義：
                      "compact_boundary" | "bridge_status" | "local_command"
                      | "away_summary" | "turn_duration"
    timestamp / sessionId / cwd / gitBranch / version
    message.content   陣列，block.type ∈ {text, thinking, tool_use, tool_result, image}
                      （亦可能是純字串，見下）
    toolUseResult     結構化 tool 結果，例：{"stdout": "..."}
    attachment.type   type=="attachment" 時的子分類

**注意：`message.content` 可能是字串也可能是陣列。** 實測兩種都有
（`{"role":"user","content":"<local-command-caveat..."}` 是字串）。
兩種都要能處理。

**事實清單 —— compact（已在 33 筆歷史 compact 上驗證）**

compact **不換檔、不換 sessionId**。掃過 294 個歷史 transcript，
sessionId 與檔名不符者 0 筆。

boundary 記錄（原文照抄關鍵欄位）：

    {"type":"system","subtype":"compact_boundary",
     "parentUuid":null,
     "logicalParentUuid":"0be38277-dafc-4882-8493-ec43fbd0f61c",
     "content":"Conversation compacted","level":"info",
     "compactMetadata":{
       "trigger":"manual","preTokens":792090,"postTokens":16995,
       "cumulativeDroppedTokens":775095,"durationMs":133856,
       "preservedSegment":{"headUuid":"...","anchorUuid":"...","tailUuid":"..."},
       "preservedMessages":{"anchorUuid":"...","uuids":[...],"allUuids":[...]}},
     "uuid":"366d2d68-af08-445e-be8d-4ddb07a7567f"}

緊接一筆 `type:"user"`、`isCompactSummary: true`、`parentUuid` 指向 boundary 的
uuid，內容以 "This session is being continued from a previous conversation" 開頭。

已驗證：N 次 compact → 檔案中有 N+1 個 root（實測 boundaries=5 的檔 roots=6）。
`trigger` 在 33 筆中全為 `"manual"`；**其他值（很可能有 `"auto"`）必須容錯，
未知值原樣保留字串，不得 panic、不得當成錯誤。**

**要交付的東西**

  pub struct TranscriptRecord {
      pub uuid: Option<String>,
      pub parent_uuid: Option<String>,
      pub logical_parent_uuid: Option<String>,
      pub is_sidechain: bool,
      pub record_type: String,
      pub subtype: Option<String>,
      pub is_compact_summary: bool,
      pub compact_metadata: Option<CompactMetadata>,
      pub raw: serde_json::Value,     // 原始 JSON，供未知型別降級顯示
  }

  pub struct CompactMetadata {
      pub trigger: String,            // 未知值原樣保留
      pub pre_tokens: Option<u64>,
      pub post_tokens: Option<u64>,
  }

  /// 解析一行。解析失敗回 Err，呼叫端跳過該行繼續。空行回 Ok(None)。
  pub fn parse_record(line: &str) -> anyhow::Result<Option<TranscriptRecord>>

  pub struct Transcript { /* uuid -> record、插入序、目前 leaf */ }

  impl Transcript {
      pub fn new() -> Self;

      /// 依序吸收新的記錄。可重複呼叫（增量 tail）。
      /// leaf 的更新規則：只要新記錄有 uuid 且 is_sidechain == false，
      /// 就成為新的 leaf。
      pub fn absorb(&mut self, records: impl IntoIterator<Item = TranscriptRecord>);

      /// 從目前 leaf 沿 parentUuid 往回走，反轉後回傳。
      /// 遇到 parentUuid == None 就停止 —— 這正是「模型現在看得到的上下文」。
      pub fn active_path(&self) -> Vec<&TranscriptRecord>;

      /// 同上，但在 subtype == "compact_boundary" 的記錄處改跟
      /// logicalParentUuid 繼續往回走，直到真正的檔案起點。
      /// boundary 記錄本身要留在結果裡（UI 要畫分界）。
      pub fn full_path(&self) -> Vec<&TranscriptRecord>;

      pub fn compact_boundaries(&self) -> Vec<&TranscriptRecord>;
  }

**硬性要求**

  - **不得做線性播放。** 顯示順序一律來自 path 走訪。
  - **`is_sidechain == true` 的記錄要吸收但不得進入 path**（P1 不顯示 subagent，
    但資料結構要能容納，P2 才不用重寫）。
  - 環狀 parentUuid（不該發生，但檔案可能損毀）必須有走訪上限或 visited set，
    **不得無限迴圈**。這一條要有測試。
  - parentUuid 指向不存在的 uuid（tail 尚未讀到、或檔案損毀）→ 走訪在該處停止，
    回傳已走到的部分，不得 panic、不得回空。
  - 未知的 `type` / `subtype` / block type 一律保留在 `raw` 裡，不得丟棄。
  - 這個模組**不得有任何檔案 I/O**。輸入只有字串與記錄。

**回歸測試**

  1. 線性 5 筆 → active_path 順序正確。
  2. 分支：uuid A 有兩個 child B、C（C 較晚吸收）→ leaf 為 C，
     active_path 含 C 不含 B。
  3. compact：root1 → ... → tail，接 boundary(parentUuid=null,
     logicalParentUuid=tail)，接 summary(isCompactSummary) → 之後三筆。
     - active_path 從 boundary 開始，**不含** compact 前的內容。
     - full_path 含 compact 前的全部內容，且 boundary 記錄在正確位置。
  4. 兩次 compact（3 個 root）→ full_path 依時序完整、active_path 只到最後一段。
  5. `trigger` 為 `"auto"` 與為 `"something-new"` 都能解析，字串原樣保留。
  6. `message.content` 為字串的記錄能解析。
  7. `isSidechain: true` 的記錄被吸收，但不出現在任何 path 中。
  8. parentUuid 環狀（A→B→A）→ 走訪終止並回傳有限結果（給明確 timeout）。
  9. parentUuid 指向不存在的 uuid → 走訪在該處停止，回傳非空的部分路徑。
 10. 未知 type `"brand-new-thing"` 解析成功，`raw` 內容完整。
 11. 增量：先 absorb 前半、取 path，再 absorb 後半、再取 path，
     結果與一次 absorb 全部相同。

**盲測**（另一隻，見 A.6）針對 3、4、8、9 這四項，只給上面的 signature 與
compact 語意描述，不給實作。


================================================================================
A.4 WP3 —— session_store.rs（非同步生命週期）
================================================================================

模型：Opus 5 high（grok 額度已用盡；xhigh 不用於 code 生成）
理由：非同步生命週期 + 檔案輪詢 + 狀態切換，規則明訂不給 agy。
相依：WP1、WP2 完成且測試綠。

**事實清單**

  - 註冊表目錄：`<home>/.claude/sessions/`，逐檔 `*.json`（忽略 `*.key`）。
  - transcript 定位：**glob `<home>/.claude/projects/*/<sessionId>.jsonl`**。
    **不得自己把 cwd 的 `/` 換成 `-` 去組路徑**（slug 規則未文件化）。
    找不到 → 該 session 標為 `transcript: None`，仍要列出來。
  - home 用 `crates/paths` 既有的 helper 取得，不要自己讀 `$HOME`。
  - 判活需要 pid 的 process start time。**macOS 讀不到其他 process 的環境變數**
    （`ps eww -p <其他 pid>` 實測回空），所以只能用 `ps -o lstart= -p <pid>`
    這類方式取得啟動時間字串，與註冊檔的 `procStart` 做字串比對。
    Linux 走 `/proc/<pid>/stat` 也可以，但**必須兩個平台都有實作**，
    不得寫成只在其中一個平台能動。

  - 輪詢（P1 用輪詢，不用 fs.watch，理由見正文 §7）：
      * 註冊表目錄       每 1000ms 重掃一次
      * 目前選中的 transcript 每 250ms `metadata()` 看 size 有無變化
      * 沒有選中的 session 不 tail
  - 增量讀取：記住 byte offset，只讀新增段落。
    **sanity check：檔案 size < 已記錄 offset → 丟棄狀態、從頭重讀。**
    最後一行可能是半行（正在寫入）→ 保留在 buffer，下次補齊再 parse。
  - **sessionId 變更偵測**：同一個 pid 的註冊檔 `sessionId` 改變（`/clear`）
    → 清空 Transcript、換檔、offset 歸零、通知 UI。
    這是主要的切檔訊號；compact **不會**觸發它（已驗證）。

**要交付的東西**

  一個 GPUI entity（`Entity<ClaudeSessionStore>`），內含：
    - `sessions: Vec<RegisteredSession>`（已判活、已排序）
    - `selected: Option<u32>`（pid）
    - `transcript: Transcript`
    - 兩個輪詢 Task，存在欄位裡（entity drop 即停止）
    - 狀態改變時 `cx.notify()`

  對 UI 只暴露：`sessions()`、`selected()`、`select(pid)`、`transcript()`。

**硬性要求**

  - 所有 fallible 操作用 `?` 或 `.log_err()`，**不得 `let _ =`**。
  - 檔案讀取一律在 `cx.background_spawn` 上，不得阻塞前景執行緒。
  - 單一 session 的檔案讀取失敗不得讓整個列表消失。
  - 不得讀取 `*.key`，不得把 `messagingSocketPath` 帶進任何結構。

**驗收要回答的兩個問題（review 清單必列）**

  - **終止性**：每個輪詢迴圈的退出條件是什麼？entity 被 drop 時 Task 真的停了嗎？
  - **錯誤復原**：一行畸形 JSON 會不會吃掉後面全部？半行處理會不會在
    「剛好切在多位元組 UTF-8 字元中間」時壞掉？（要有測試）


================================================================================
A.5 WP4 —— claude_sessions_panel.rs（一整頁）
================================================================================

模型：Opus 5 high（grok 額度已用盡）。規則上「任何一整頁」原本派 grok high。
相依：WP3。

**事實清單**

  - Panel 結構、`Panel` trait 實作、`load()` 的形狀，抄
    `crates/tmux_sessions/src/tmux_sessions_panel.rs`（413 行）。
    forward_ports_panel.rs（973 行）是比較複雜的參考。
  - markdown render 用既有的 `crates/markdown`：
      `Markdown::new(...)` (markdown.rs:646)
      `Markdown::new_text(source, cx)` (markdown.rs:712)
      `Markdown::append(&mut self, text, cx)` (markdown.rs:997)
      `Markdown::reset(&mut self, source, cx)` (markdown.rs:1042)
      `MarkdownElement::new(markdown_entity, style)` (markdown.rs:1640)
  - 圖片：`gpui::Image { format, bytes, id }`（crates/gpui/src/platform.rs:2707）。
    參考 agent panel 對 `ContentBlock::Image` 的處理
    （crates/acp_thread/src/acp_thread.rs:1746）。
  - **只用既有 `IconName` 的值，不得新增圖示資產。**

**畫面**

  上半：session 列表（name、cwd 相對顯示、version、status 點、
        bridge 圖示（`bridgeSessionId` 有值時）、tmux target 灰字）。
  下半：選中 session 的對話流，虛擬捲動。

  對話流的 render 規則：
    - `text` block         → markdown
    - `thinking` block     → 預設折疊
    - `tool_use` /
      `tool_result`        → 折疊卡片。優先用 top-level `toolUseResult`
                             （結構化）而不是文字 content 做 render。
    - `<persisted-output>` marker（見正文 §1.4）→ 顯示 preview，
                             加「載入完整輸出」按鈕，按下才去讀那個絕對路徑。
    - `attachment`         → 全部收進一個折疊的 context 區，不平鋪在對話流。
    - `compact_boundary`   → 一條明確分界，顯示 trigger 與
                             `preTokens -> postTokens`；分界以上的歷史
                             **預設折疊**，展開時改用 `full_path()`。
    - `local_command`      → content 含 ANSI escape，**要 strip 後再顯示**。
    - 未知 type / block    → 降級成 raw JSON 折疊區。**不得丟棄、不得 panic。**

**硬性要求**

  - 唯讀。P1 不放輸入框（或放但停用並標示「P3」）。
  - 大 transcript（實測單檔 2.7MB / 2695 筆）必須虛擬捲動，
    不得整份 render。切換 session 時不得整份重 parse。


================================================================================
A.6 盲測
================================================================================

模型：Opus 5 medium。**不可降。**

  - 只給 WP1 / WP2 的 signature 與語意描述（A.2、A.3 的「要交付的東西」與
    「硬性要求」兩段，逐字複製），**禁止看實作**。
  - 寫好之後 held out：**實作者與 fixer 都不准讀、不准跑、不准改。**
  - 重點涵蓋 WP2 的 3、4、8、9（compact 兩段路徑、環狀、斷鏈）。
  - 回歸測試才住在套件裡；盲測放在 held-out 位置由大腦跑。


================================================================================
A.7 派工樣板
================================================================================

（grok 額度用盡後，本任務書只剩兩種派工對象：Workflow 的 `agent()`（Opus）與 agy CLI。
下面的 `agent` CLI 段落先留著，等額度回來直接可用。）

每一份 subagent / agy / agent prompt 開頭逐字貼：

  第一步先跑 `ToolSearch("select:mcp__codegraph__codegraph_explore")`，再用
  `codegraph_explore` 查符號定義／回傳形狀／呼叫者／blast radius／測試覆蓋，
  一次問完。配額用完改走 shell `codegraph explore "<符號>"`。
  grep 只准用於字串常數、檔案列舉、build 產物。

（agy / agent 這類 CLI 子行程沒有 MCP，改貼：）

  先用 shell `codegraph explore "<符號或問題>"` 查符號定義／回傳形狀／呼叫者／
  blast radius／測試覆蓋，一次問完。grep 只准用於字串常數、檔案列舉、build 產物。

結尾逐字貼：

  發現規格自相矛盾、或你正在挑輸入／加資料讓某個測試過，停下來回報，
  不要換輸入。回報「做不到」是成功的結果。
  禁止 `git stash` / `git checkout` / `git restore` / `git reset`。


================================================================================
A.8 依賴順序與驗收
================================================================================

    WP0 ──┬── WP1 ──┐
          └── WP2 ──┴── WP3 ── WP4
                 └── 盲測（與 WP1/WP2 並行，不同隻，不看實作）

每個 WP 結束跑機械 gate（大腦在主迴圈跑，不派給 agent）：

    ./script/clippy                      ← 不是 cargo clippy
    cargo test -p claude_sessions
    codegraph affected <本輪改動的檔案>   ← 跑出來的測試檔也要跑

review loop 依 CLAUDE.md：每輪一隻 agent 從頭做到尾（找缺陷 → 落 RED 測試 →
修到綠），乾淨 context、對抗立場。

**跨家族 review 這一輪做不到**：grok 額度已用盡，剩下的另一個家族只有
agy `gemini-3.8-flash-high`，而對抗式 review 不屬於它能勝任的「事實清單寫得死」那格。
折衷做法：

  - WP2 / WP3 至少各兩輪，兩輪都是 Opus，但**必須**乾淨 context、對抗立場，
    且第二輪換 effort（第一輪 high、第二輪 medium 走不同的注意力分布）。
  - 兩輪都不得由實作者本人執行。
  - 額外補一輪 agy `gemini-3.8-flash-high` 的**機械對帳**（不是 review）：
    檢查 `git diff` 的每一處 `src/` 改動是否都對得上一條 finding、
    findings 數／RED 測試數／修正條目數是否對得起來。這是它能做的部分。
  - grok 額度回來之後，WP2 與 WP3 要補一輪真正的跨家族 review。**這條記在這裡別忘。**

P1 完成的定義：
  - Zed 能列出本機所有活著的 interactive claude session。
  - 選一個能看到完整對話，含圖片、markdown 表格、折疊的 tool call。
  - 對方在 tmux／手機／網頁打字，Zed 這邊即時跟上。
  - 對方 `/compact`，Zed 畫出分界、預設折疊舊歷史、可展開看完整。
  - 對方 `/clear`，Zed 自動切到新 session 而不是卡在舊對話。


================================================================================
================================================================================
附錄 B：規格修訂（review 輪 1 之後，由大腦裁決）
================================================================================
================================================================================

review 輪 1 升級了三條「規格沒決定」的東西。以下是裁決，實作與盲測都以此為準。

--------------------------------------------------------------------------------
B.1 修訂（對應 F9）：persisted output 的辨識改用結構化欄位
--------------------------------------------------------------------------------

**問題**：正文 §1.4 說用 `<persisted-output>` marker 文字去認卸載的大型輸出。
但實作出來之後，整條路徑在生產環境是死碼——因為 `toolUseResult`（結構化）
勝過 content block，而 `toolUseResult.stdout` 裡沒有 marker。

**實測資料（2026-09-10，掃全部歷史 transcript）**

    toolUseResult 帶 persisted* 欄位的記錄：86 筆
      persistedOutputPath：85 筆
      persistedOutputSize：85 筆
    tool_result block 的 content 含 <persisted-output> marker：86 筆
    樣本：
      persistedOutputPath = ".../tool-results/b7vd9w357.txt"
      persistedOutputSize = 82585
      stdout 長度         = 29999   ← 被截在 30000，**不含 marker**
      resultKeys = [stdout, stderr, interrupted, isImage,
                    noOutputExpected, persistedOutputPath, persistedOutputSize]

**裁決：不是二選一，兩個都要。** review 輪提的 (a) 保留 stdout 內嵌、
(b) 改用 marker preview 加載入按鈕——兩者都比正確答案差：

  - 內嵌本文用 `toolUseResult.stdout`（實測 30 KB），**不是** marker 的 2 KB preview。
    這是使用者立刻看到的東西，30 KB 遠勝 2 KB，也遠勝 terminal。
  - **同時**，當 `persistedOutputPath` 存在時，顯示「Load full output」按鈕，
    大小標示用 `persistedOutputSize`（實測 82,585），按下才讀那個路徑。
  - 也就是說「內嵌 preview」與「可載入完整」是兩件獨立的事，不互斥。
    原規格把它們綁在一起才產生了死碼。

**路徑淨化（F3 已修的那條，這裡再收緊一次）**

  - 路徑**只能**來自 `toolUseResult.persistedOutputPath`，或
    `type == "attachment"` 記錄的 marker 文字（hook 輸出走這條）。
  - **絕對不得**從 `message.content` 的文字（user text、assistant text）解析路徑。
    實測 marker 從不出現在那裡，任何在那裡的 marker 都是偽造或巧合。
  - 讀檔前必須驗證：路徑是絕對路徑，且位於
    `<home>/.claude/projects/` 之下。不符就不顯示按鈕（顯示原文即可），
    不要顯示一個會失敗的按鈕。
  - marker 文字解析保留為 fallback（85/86 有結構化欄位，那 1 筆與 attachment 需要它）。

--------------------------------------------------------------------------------
B.2 修訂（對應 F11）：visible_sessions 的排序平手規則
--------------------------------------------------------------------------------

**問題**：正文 §2 與附錄 A.2 定義排序 key 為
`(是否屬於本專案 == false, name 字串序)`，沒有定義平手時怎麼排。
於是同名（或同為 None）的 session 繼承 `read_dir` 的任意順序，
每次輪詢都可能互換，UI 列會閃動。

**裁決：加第三段 tiebreaker `process_id` 遞增。**

    排序 key = (是否屬於本專案 == false, name 字串序, process_id 遞增)

理由：`process_id` 在活著的 session 之間唯一、在 session 生命期內不變、
是數值比較不需要正規化。`session_id` 也唯一但沒有比 pid 更好的性質。

`name` 為 `None` 時排序仍當空字串（原規則不變）。

--------------------------------------------------------------------------------
B.3 修訂（對應 F8）：對話 entry 要以 record uuid 為鍵快取
--------------------------------------------------------------------------------

**問題**：正文 §5 與附錄 A.5 只寫了「必須虛擬捲動」「切換 session 時不得整份重
parse」，沒有寫「同一個 session 收到新資料時不得整份重建」。
於是實作每次 store 通知都在前景執行緒走完整條 path 重建所有 entry：
每張圖重新 base64 解碼（貼上的截圖每張約 1.5 MB）、每筆 unknown 記錄重新
pretty-print JSON，然後再逐位元組比較找出差異。串流中每秒最多 4 次。

**裁決：規格補一條——昂貴的 per-record 產物必須以 record uuid 為鍵快取，
跨重建存活。**

  - 快取鍵：`record uuid`（+ block index，因為一筆記錄可以有多個 block）。
  - 必須跨重建存活的產物：base64 解碼後的圖片位元組、pretty-print 的 raw JSON、
    ANSI strip 後的文字、markdown 來源字串。
  - 記錄一旦吸收就不會再變（append-only + uuid 覆蓋是罕例），
    所以快取不需要失效策略；只需要在切 session／transcript restart 時整份丟棄。
  - 同 uuid 被覆蓋（`absorb` 允許）時要丟該鍵的快取。
  - 這條**不是**效能優化的建議，是正確性層級的要求：
    沒有它，本功能在它最該發光的場景（長 session、貼了截圖）會卡。

--------------------------------------------------------------------------------
B.4 明確留給 P2 的（不在 P1 範圍）
--------------------------------------------------------------------------------

  - review 輪 1 的 F10（decode_base64 對畸形輸入靜默寬鬆）、F12（registry 與
    transcript 共用一個 error 欄位，前者成功會清掉後者的訊息）——標 wontfix，
    嚴重度低於門檻，記錄在此。
  - grok 額度回來之後，`transcript.rs` 與 `session_store.rs` 要補一輪
    真正的跨家族 review（見 A.8）。


================================================================================
================================================================================
附錄 C：P1 完成記錄（2026-09-10）
================================================================================
================================================================================

--------------------------------------------------------------------------------
C.1 交付狀態
--------------------------------------------------------------------------------

    cargo test -p claude_sessions   140 passed / 0 failed
                                      ├─ 71 條 held-out 盲測（語意從未被修改，
                                      │   去空白 hash 與原始副本相同）
                                      └─ 69 條回歸測試
    ./script/clippy                 綠（--workspace --release --all-targets
                                       --all-features -- --deny warnings）
    cargo build -p zed              綠
    README.md 的 > [!IMPORTANT] 兩行  完好

crates/claude_sessions/ 共 6366 行（含測試）：

    claude_sessions.rs            35   crate root / actions / init
    claude_sessions_button.rs     57   status bar 按鈕（IconName::AiClaude）
    transcript.rs                671   純函式：記錄解析與對話樹
    session_registry.rs          410   純函式：註冊表解析與判活
    session_store.rs            1252   非同步：探索、tail、生命週期
    claude_sessions_panel.rs    2571   Panel UI
    blind_registry_tests.rs      587   held-out
    blind_transcript_tests.rs    783   held-out

--------------------------------------------------------------------------------
C.2 執行過程
--------------------------------------------------------------------------------

WP0（crate 骨架 + wiring）由大腦在主迴圈完成——碰 Cargo.toml / zed.rs / main.rs
三個共用檔，風險不外派。

WP1  agy gemini-3.8-flash-high
WP2  Opus 5 high（Workflow）        ← grok 額度用盡，原表派 grok xhigh
WP3  Opus 5 high（Workflow）        ← 同上
WP4  Opus 5 high（Workflow）        ← 原表派 grok high
盲測  Opus 5 medium（Workflow），派工時 transcript.rs 仍為 placeholder，
      物理上看不到實作
review 輪 1        Opus 5 high，乾淨 context、對抗立場
規格修訂 B.1–B.3   Opus 5 high
最終 spec 比對     Opus 5 high

findings 對帳：

    review 輪 1     12 條 = fix 7 + wontfix 5   測試 122 → 130
    規格修訂        3 條（B.1/B.2/B.3）          測試 130 → 137
    最終 audit      6 條 = fix 3 + wontfix 3     測試 137 → 140

比對表統計：DONE 61、MISSING（已修）2、NO_TEST（wontfix）1、OUT_OF_P1 4。

--------------------------------------------------------------------------------
C.3 大腦的突變抽查（不採信 agent 回報，自行驗證）
--------------------------------------------------------------------------------

每一輪交付後，大腦都把「應該被測試釘住的行為」故意改壞，確認測試真的會紅。
這是唯一能證明測試不是空轉的方法。

    突變                                         結果
    ─────────────────────────────────────────────────────────────────────
    full_path 不跟 logicalParentUuid            4 個盲測 + 3 個自有測試變紅  ✓
    walk 的 sidechain 過濾拿掉                   1 個盲測變紅                 ✓
    absorb 的 sidechain leaf 守衛拿掉             【122 個測試全綠】← 缺口
      → 列為 F1 交給 review 輪落成 RED
      → 補測試後重跑：a_sidechain_record_must_not_become_the_leaf 變紅  ✓
    忽略 is_compact_summary                      1 個測試變紅                 ✓
    attachment 不過路徑界限                       1 個測試變紅                 ✓

F1 是整個 P1 唯一一個「實作正確但沒有任何測試守著」的行為，而且它只在
大腦做突變抽查時才浮現——71 條盲測與 27 條回歸測試都沒抓到，因為 walk 的
過濾遮蔽了 absorb 的錯誤。review 輪 1 另外實測 828 份真實 transcript，
證明它的嚴重度比大腦初判的「低」更高：375 份含 sidechain 記錄的 transcript
全部以 sidechain 結尾，突變下會回傳空 path，45% 的真實 transcript 會顯示「無訊息」。

--------------------------------------------------------------------------------
C.4 剩餘項（全部有明確歸屬，沒有一項是「P1 沒做完」）
--------------------------------------------------------------------------------

**效能，非正確性風險（修法都要改 WP3/WP4 的交付形狀，屬設計決定）**

  G4  `Entry` 的 derive `PartialEq` 對 `Arc<Image>` 比值不比指標，
      所以 rebuild 的 prefix/suffix 掃描會 memcmp 每張已解碼的截圖。
      修法要手寫 `PartialEq` 讓圖片走 `Arc::ptr_eq`，會動到 splice 邏輯
      賴以建立的相等語意。
  G6  新選中的 transcript 第一批在前景執行緒 parse（2.7 MB / 2695 筆）。
      A.4 的硬性要求是「檔案讀取不得阻塞前景」，那一條沒有違反；
      這是每次選取一次性的數十毫秒。

**無測試但推導不出錯誤輸出**

  G5  B.3 的「切 session／transcript restart 整份丟棄快取」實作正確但無測試。
      要釘它得把整個 Workspace + Project 立起來（現有 panel 測試全是
      build_entries 層的純函式測試）。缺這個 guard 也推導不出錯誤輸出：
      uuid 全域唯一 + §3.3 保證檔案不被改寫 + retain_keys 已收住記憶體。
      **這是本 crate 唯一的 NO_TEST，明確記錄在此而非默默放掉。**

**已知殘留**

  - `persisted_output_is_loadable` 不做 `canonicalize`（純函式不做 I/O），
    所以界限內指向界限外的 symlink 不攔。
  - F10 `decode_base64` 對畸形輸入靜默寬鬆（合法輸入正確，畸形最後都是空的
    或被 ImageFormat 拒收）。
  - F12 registry 與 transcript 共用一個 error 欄位，前者成功會清掉後者的訊息。

**流程上欠的一輪**

  - CLAUDE.md 要求 WP2 / WP3 的第二輪 review 換不同家族的模型。
    grok 額度用盡，這一輪做不到；effort 換擋只是弱代理，不是等價替換。
    **額度回來之後 transcript.rs 與 session_store.rs 要補一輪真正的
    跨家族 review。** 這條不影響 P1 的功能完整性，但它是這次交付
    唯一沒有滿足的流程要求。

--------------------------------------------------------------------------------
C.5 下一步
--------------------------------------------------------------------------------

P2（subagent / workflow 檢視，見正文 §4）的資料層在 P1 已經備好：
`transcript.rs` 吸收 `isSidechain` 記錄但不讓它進 path，所以 P2 只要
讀 `<sessionId>/subagents/agent-<id>.{jsonl,meta.json}` 並用 `toolUseId`
掛回主對話，不需要重寫任何 P1 的東西。


================================================================================
附錄 D 實機驗證後的規格修訂（2026-09-10）
================================================================================

P1 交付後在本機開著 7 個 session 實跑，面板顯示
「No Claude Code sessions are running on this machine.」。
兩個原因都是**規格寫錯**、實作照規格做，測試也照規格寫，所以三邊一致地錯。

--------------------------------------------------------------------------------
D.1 `procStart` 是 UTC，`ps -o lstart=` 是本地時區
--------------------------------------------------------------------------------

正文 §2 與 A.2 事實清單只寫「與註冊檔的 `procStart` 做字串比對」，
沒有寫時區。實機量到：

  registry procStart : Thu Sep 10 02:53:02 2026
  ps -o lstart=      : Thu Sep 10 10:53:02 2026     ← 本地 (Asia/Taipei, +8)
  TZ=UTC ps -o lstart=: Thu Sep 10 02:53:02 2026    ← 相符

7 個註冊檔（CLI 版本 2.1.257 ~ 2.1.267）全部相差正好 8 小時，
即 Claude Code 寫入的是 UTC 呈現。字串比對永遠不相等 →
每一個 session 都被判 `PidReused` → 全部被 `visible_sessions` 濾掉。

**修訂**：取 process start time 的指令必須固定在 UTC 執行
（`command.env("TZ", "UTC")`）。回歸測試
`test_process_start_times_are_reported_in_utc`：spawn 一個真的 process、
把回傳字串以 UTC 解析成 epoch、與 spawn 當下的時間比對，容差 5 秒。
修之前紅在 `off by 28800 seconds`。

  註：這支測試不能用 `#[gpui::test]`——await 真的 `ps` 會踩到
  GPUI deterministic scheduler 的 "Parking forbidden"。改成一般
  `#[test]` + `smol::block_on`。

--------------------------------------------------------------------------------
D.2 `updatedAt` 不是 heartbeat，不得當判活條件
--------------------------------------------------------------------------------

正文 §1.1 寫「`updatedAt` → heartbeat；太舊視為 stale」，§2 判活第 2 條
要求「`updatedAt` 在合理範圍內」，A.2 據此定 `STALE_HEARTBEAT_MILLIS = 60_000`。
實機 7 個註冊檔**每一個** `updatedAt == statusUpdatedAt`，也就是它只在
session 狀態改變時被改寫，不是定時心跳：

  web-tools-16    status=idle   updatedAt 距今 3336s   process 活著
  dashboard-0f    status=busy   updatedAt 距今  284s   process 活著
  phoneoncloud-81 status=idle   updatedAt 距今 138842s process 活著

等在使用者輸入前的 idle session、或卡在一個長 tool call 的 busy session，
都會好幾小時不動 `updatedAt`。所以 60 秒門檻即使在 D.1 修好之後，
仍然只會留下「剛剛改過狀態」的那一個 session。

**修訂**：判活只看 process 本身（pid 存在 **且** `procStart` 相符）。
`updatedAt` 只能拿來顯示，不得作為隱藏 session 的依據。
實作上 `liveness()` 的 API 與既有測試（含盲測）都不動——門檻由呼叫端給，
`session_store` 改傳 `HEARTBEAT_CUTOFF_MILLIS = i64::MAX`。
回歸測試 `test_a_session_that_has_not_changed_status_for_an_hour_is_still_listed`。

  `Liveness::StaleHeartbeat` 這個 variant 因此在 production 走不到。
  保留它是因為 `liveness()` 是純函式、門檻是參數，變體本身沒有錯；
  錯的是正文對 `updatedAt` 語意的假設。

--------------------------------------------------------------------------------
D.3 實機驗收結果
--------------------------------------------------------------------------------

用 production code path（`read_registrations` → `process_start_times` →
`visible_sessions` → `find_transcript`）對真的 `~/.claude/sessions` 跑過：

  registrations read: 7      visible sessions: 7      liveness 全部 Live
  transcript 找到 6 / 7

唯一沒有 transcript 的是 `andy-type-50`（`entrypoint: "sdk-ts"`，
SDK 起的 session）——`~/.claude/projects/*/9171f86a-….jsonl` 確認不存在，
不是 glob 寫錯。這正是正文 §2 步驟 4 要求的「標成 no transcript
而不是丟掉」的情形。

--------------------------------------------------------------------------------
D.4 這次沒有改、但使用者問到的事
--------------------------------------------------------------------------------

  - **「應該要從 tmux 找 claude session」**：探索來源仍是註冊表，不是列 tmux。
    理由見正文 §2 的實測陷阱——`pane_current_command` 顯示的是版本字串
    `2.1.267` 而不是 `claude`，而且 tmux 那邊也拿不到 `sessionId`。
    註冊檔的 `tmux` 欄位（如 `"awp:@1.%1"`）反而是從 session 找到 pane 的
    可靠方向，P3 送輸入要用它。實機 7 個 session 有 5 個帶 `tmux`
    （本 session 與 SDK session 沒有）。
  - **remote**：目前只讀本機 `~/.claude/sessions`，開 remote project 時
    列的仍是本機 session。remote 是 P4，未做。
  - **read-only**：P1 就是唯讀，面板已標
    「Read-only. Replying from Zed is out of scope for this panel (P3).」
    從 Zed 打字回覆是 P3，未做。


================================================================================
附錄 E P3 + P4 任務書（2026-09-10 訂）
================================================================================

--------------------------------------------------------------------------------
E.0 共同規則（每一份派工都要逐字帶）
--------------------------------------------------------------------------------

  - 禁止 `git stash` / `git checkout` / `git restore` / `git reset`。
  - 禁止任何 lint 抑制（`#[allow(...)]` / `#![allow(...)]` / `#[expect(...)]`）。
  - 禁止 `unwrap()` / `expect()`（測試中可用）、禁止 `let _ =` 吞錯。
  - 盲測檔（`blind_*_tests.rs`）不得讀、不得跑、不得改。
  - 不得修改本 spec 檔；發現規格自相矛盾停下來回報。
  - 正在挑輸入／改測試讓某個 case 過 → 停下來回報。回報「做不到」是成功的結果。
  - `README.md` 前兩行 `> [!IMPORTANT]` / `> Remove this line...` 不得移除。
  - `~/.claude/sessions/<pid>.<sha>.key` 與 `messagingSocketPath`：不讀、不記、不傳。
  - gate：`cargo test -p <動到的 crate>`、`./script/clippy`、`cargo fmt --check`。

--------------------------------------------------------------------------------
E.1 架構決策：共用層放 `crates/remote/src/claude_sessions.rs`
--------------------------------------------------------------------------------

P4 的 server 端要在遠端做「掃註冊表 + 判活 + glob transcript + tail + 送輸入」。
`remote_server` 不能依賴 `claude_sessions`（那是 UI crate，會拉進
`ui` / `workspace` / `markdown` / `theme`）。判活邏輯剛剛才修過兩個 bug（附錄 D），
複製兩份必然走歪，所以**移動**而不是複製。

落點沿用本 repo 既有先例 `crates/remote/src/tmux_sessions.rs`
（「本機執行的機器上的 tmux」，同時被 remote_server 與 client 用）。
`remote` 已有 anyhow / collections / serde / serde_json / util / paths / smol / gpui，
不需要新增依賴，且 `remote` 不依賴 `ui`/`workspace`/`project`，不會有循環。

Client 端 `claude_sessions` 抽 source trait（list / tail / read_blob / send_input），
local 直接呼叫共用層，remote 走 proto，UI 完全不分本地遠端（正文 §7 要求）。

--------------------------------------------------------------------------------
E.2 WP5 —— 共用層搬移 + P3 純函式（Opus 5 high）
--------------------------------------------------------------------------------

新檔 `crates/remote/src/claude_sessions.rs`，在 `crates/remote/src/remote.rs`
加 `pub mod claude_sessions;`。

**搬進來（盡量逐字，不要順手改行為）**

  自 `crates/claude_sessions/src/session_registry.rs`（整檔，含 `mod tests`）：
    `RegisteredSession` / `parse_registered_session` / `Liveness` / `liveness` /
    `visible_sessions`
  自 `crates/claude_sessions/src/session_store.rs`：
    `read_registrations`(406) / `process_start_times`(486,528 兩個 cfg) /
    `normalize_whitespace`(532) / `now_millis`(538) / `find_transcript`(549) /
    `read_transcript_tail`(566) / `split_complete_lines`(386) /
    `TailState`(76) / `TailProgress`(82) / `HEARTBEAT_CUTOFF_MILLIS`(35)
    以及它們對應的既有測試（`test_split_complete_lines_*`、
    `test_read_transcript_tail_*`、`test_normalize_whitespace_*`、
    `test_read_registrations_treats_a_missing_directory_as_no_sessions`、
    `test_process_start_times_are_reported_in_utc`）。

  留在 `claude_sessions`：`ClaudeSessionStore`（gpui 生命週期）、`transcript.rs`、
  panel、button、`resolve_visible_sessions` 與 `ProcessStartLookup`（gpui `Task`）。

**盲測相容（硬性）**

  `blind_registry_tests.rs` 寫的是 `use crate::session_registry::*;`。
  那個檔一個字都不准動，所以 `crates/claude_sessions/src/claude_sessions.rs`
  要留下這條路徑：`pub use remote::claude_sessions as session_registry;`
  （原 `session_registry.rs` 檔案刪除）。

**新增（P3 用，純函式優先）**

  /// 註冊檔的 `tmux` 欄位形如 `awp:@1.%1`（session:@window_id.%pane_id）。
  /// pane id 在整個 tmux server 內唯一，實測 `tmux display-message -t %1` 可解析，
  /// 所以只取 `%<digits>` 當 target，完全避開 session 名的引號問題。
  /// 形狀不符 → None（呼叫端據此停用輸入框）。
  pub fn pane_target(tmux_field: &str) -> Option<String>

  /// 送一段（可含換行的）文字進 pane，然後送 Enter。
  /// 實測 tmux 3.6b：`load-buffer -b <name> -` 吃 stdin（文字不經過 shell），
  /// `paste-buffer -d -p -b <name> -t <pane>` 的 `-p` 是 bracketed paste，
  /// 多行才不會被 TUI 當成多次送出。
  pub async fn send_text(pane_target: &str, text: &str) -> Result<()>

  /// 中斷：`send-keys -t <pane> Escape`
  pub async fn send_escape(pane_target: &str) -> Result<()>

  /// server 端與 local source 共用的聚合：掃註冊表 + 判活 + 解析 transcript 路徑。
  pub struct SessionSummary { pub session: RegisteredSession, pub transcript_path: Option<PathBuf> }
  pub async fn list_sessions(home_directory: &Path, project_root: Option<&Path>) -> Result<Vec<SessionSummary>>

  home 用 `crates/paths` 的 helper，不要自己讀 `$HOME`；remote 端要 resolve 遠端的 `~`。

**硬性要求**

  - `pane_target` 只接受 `%` 後面全是數字的尾段；`-`、空白、空字串一律 None
    （值來自 JSON 檔，視為不可信輸入，且不得讓它變成 `tmux` 的旗標）。
  - 送輸入的三個指令一律用 argv 傳參，不得組 shell 字串。
  - 文字只走 stdin，不得放進命令列。
  - `send_text` / `send_escape` 失敗要回 Err 並帶 stderr（UI 要顯示得出來）。
  - 不得自動送任何東西：這兩支只在使用者按下按鈕時被呼叫。

**回歸測試**：`pane_target` 的正例（`awp:@1.%1` → `%1`、`a:b:@10.%234` → `%234`）
與反例（`awp:@1`、`awp:@1.%`、`awp:@1.%x`、`-t`、``、`%1 ; rm -rf /`）；
搬過來的測試全部維持綠。

--------------------------------------------------------------------------------
E.3 WP6 —— source trait + local source + store 改接 + P3 UI（Opus 5 high）
--------------------------------------------------------------------------------

新檔 `crates/claude_sessions/src/session_source.rs`：

  pub enum SessionInput { Text(String), Escape }

  pub trait SessionSource: Send + Sync + 'static {
      fn list_sessions(&self, project_root: Option<PathBuf>) -> Task<Result<Vec<SessionSummary>>>;
      fn tail(&self, path: PathBuf, offset: u64) -> Task<Result<FileChunk>>;
      fn read_blob(&self, path: PathBuf, max_bytes: u64) -> Task<Result<Vec<u8>>>;
      fn send_input(&self, pane_target: String, input: SessionInput) -> Task<Result<()>>;
  }

  pub struct FileChunk { pub bytes: Vec<u8>, pub next_offset: u64, pub restarted: bool }

  `LocalSource`（本波做）：直接呼叫 `remote::claude_sessions::*`。
  `RemoteSource`（WP9 做）：本波只留 trait 與 `LocalSource`。

`ClaudeSessionStore`：
  - 建構子改成 `new(project_root, source: Arc<dyn SessionSource>, cx)`。
  - registry / transcript 輪詢改成走 source，不再自己讀檔。
  - 新增 `pub fn send_input(&mut self, input: SessionInput, cx)`：
    取目前選中 session 的 `tmux_target` → `pane_target()`；None 則不送、設 error。
  - 既有測試改用一個 fake source（原本的 `fake_process_start_lookup` 那條路留著）。

`ClaudeSessionsPanel`（P3 UI）：
  - 底部把 `render_read_only_footer` 換成輸入區：多行輸入 + 「Send」+「Esc」。
  - 選中的 session 沒有可用 pane target → 輸入區停用，顯示原本的唯讀說明。
  - 送出成功清空輸入框；失敗把錯誤顯示在既有 error 位置。
  - **送出只能由使用者觸發**（按鈕或 Enter），不得有任何自動送出路徑。
  - `IconName` 只用既有的，不要新增 asset。

--------------------------------------------------------------------------------
E.4 WP7 —— proto + remote_server handler（Gemini 3.8 Flash high，機械性）
--------------------------------------------------------------------------------

樣板逐字對照 `ListTmuxSessions`：
`crates/proto/proto/tmux.proto`、`crates/proto/proto/zed.proto:18,533-534`、
`crates/proto/src/proto.rs:191-192,498`、
`crates/remote_server/src/headless_project.rs:307,1357-1389`。

新檔 `crates/proto/proto/claude_sessions.proto`：

  message ClaudeSession { uint32 process_id=1; string session_id=2; string working_directory=3;
    string version=4; optional string name=5; optional string status=6; optional int64 updated_at=7;
    optional string tmux_target=8; optional string transcript_path=9; }
  message ListClaudeSessions { uint64 project_id=1; optional string project_root=2; }
  message ListClaudeSessionsResponse { repeated ClaudeSession sessions=1; }
  message TailClaudeFile { uint64 project_id=1; string path=2; uint64 offset=3; uint64 max_bytes=4; }
  message TailClaudeFileResponse { bytes contents=1; uint64 next_offset=2; bool restarted=3; }
  message SendClaudeInput { uint64 project_id=1; string pane_target=2; oneof input { string text=3; bool escape=4; } }

  zed.proto：`import "claude_sessions.proto";` + oneof 501..506（`// current max` 註解要跟著移）。
  proto.rs：`(Msg, Background)` 逐個登記，request/response 配對兩組
  （`SendClaudeInput` 回 `Ack`）。
  handler 三支照 `handle_list_tmux_sessions` 的形狀，body 只呼叫
  `remote::claude_sessions::*`，`cx.background_spawn` 包住 IO。
  home 在 server 端 resolve。

  **硬性**：不得把 raw JSON 丟給 client 解（正文 §7）；相容性處理留在產生端。
  **不得**新增 proto 欄位以外的東西，不得改既有訊息編號。

--------------------------------------------------------------------------------
E.5 WP9 —— RemoteSource（Gemini 3.8 Flash high，機械性）
--------------------------------------------------------------------------------

`session_source.rs` 補 `RemoteSource { client: AnyProtoClient }`，三支 RPC 對接
WP7 的訊息；panel 建 store 時依 `project.read(cx).remote_client()` 選 source
（有 → RemoteSource，無 → LocalSource）。

--------------------------------------------------------------------------------
E.6 盲測（Opus 5 medium，禁看實作）
--------------------------------------------------------------------------------

只給 E.2 的 API 與語意，**不准讀** `crates/remote/src/claude_sessions.rs`
與 `crates/claude_sessions/src/`。寫成
`crates/claude_sessions/src/blind_input_tests.rs`（大腦負責掛 `mod`），
覆蓋 `pane_target` 的淨化與 `FileChunk` 的 offset 語意
（offset 前進、檔案縮小要 restarted、offset 超過檔尾）。

--------------------------------------------------------------------------------
E.7 順序與驗收
--------------------------------------------------------------------------------

  Wave 1  WP5 ∥ 盲測          Wave 2  WP6 ∥ WP7
  Wave 3  WP9                 Wave 4  大腦驗收 + 乾淨 context Opus 對抗式 review
                                      （findings JSON → RED → 修到綠）

每一波之間由大腦跑 gate（test / clippy / fmt / `cargo build -p zed`
/ `cargo build -p remote_server`）。收工前用真的 `~/.claude/sessions` 實跑一次，
P3 的實際送出由使用者自己在 UI 上驗（agent 不得對真的 session 送任何輸入）。

--------------------------------------------------------------------------------
E.8 驗收發現：tail RPC 的任意檔案讀取（已修）
--------------------------------------------------------------------------------

WP7 交付後我驗 diff 發現的洞，嚴重度足以當場修，不留到 Wave 4。

`read_transcript_tail(home, session_id, state)` 的語意是「`state.path` 存在就照著讀，
否則才 glob」。這在 local 沒問題（path 是自己上一輪 glob 出來的），
但 `TailClaudeTranscript.path` 是 **client 傳來的**，於是 handler 會照著讀任何
remote server 讀得到的檔案。取證（暫時測試，跑完已還原）：

  tail returned lines ["sk-secret-value-that-must-never-be-read"]
    from Some("/var/folders/.../probe-not-a-transcript.key")

也就是 `~/.claude/sessions/<pid>.<sha>.key` 可以被這條路徑讀出來 ——
正文 §1.1／§8 明文「不讀、不記、不傳」的那個檔。

**修法**（在 server 端信任邊界，`headless_project.rs`）：
新增純函式 `transcript_path_to_follow(path, session_id, home)`，client 傳來的 path
必須同時滿足：絕對路徑、位於 `<home>/.claude/projects/` 之下、不含 `..` component、
檔名正好是 `<session_id>.jsonl`。不符就當成 `None`（不是回錯），
讓 server 用 session_id 自己重新 glob —— 語意上正好等於「你追的檔不見了，重找」。

**三個 clause 各自有一個只有它抓得到的案例，逐一突變都會變紅**：

  拿掉 `starts_with(projects)`  → `<home>/.claude/sessions/<session>.jsonl` 這案變紅
  拿掉檔名檢查                 → `projects/x/another-session.jsonl` 這案變紅
  拿掉 `..` 檢查               → `projects/x/../../.claude/sessions/<session>.jsonl` 這案變紅

**「什麼合法輸入會被誤殺」**：拿本機真的 7 個 session 跑 production 路徑
（`list_sessions` → `transcript_path_to_follow`），6 個有 transcript 的全部通過，
第 7 個本來就沒有 transcript（SDK session）。誤殺 0 筆。

  註：`paths::home_dir()` 在 `cfg(test)` / `feature = "test-support"` 下固定回
  `/Users/zed`（見 crates/util/src/paths.rs:27），所以測試裡拿不到真的 home，
  實機驗證要自己傳 `$HOME`。production 不受影響。

--------------------------------------------------------------------------------
E.9 WP6 回報的五個規格問題 —— 大腦裁定
--------------------------------------------------------------------------------

1. **「送出成功後清空輸入框」字面上做不到，且照字面做會更糟。**
   WP6 指出：等 tmux 三個子指令跑完（數十毫秒）才清空，會把使用者這段期間
   新打的字一起吃掉。它的實作是交付給 source 的當下就清空。
   **裁定：清空時機維持不變（交付即清），但送出失敗必須把文字還回輸入框，
   且只在輸入框當下仍為空時才還**（使用者又打了字就不准蓋掉）。
   這條列進 WP9 施工，附三支測試（失敗且空→還回、失敗但已有新內容→不覆蓋、成功→保持空）。

2. **`error` 加來源標記（`ErrorSource::Poll` / `Send`）：接受。**
   這不是超出最小差異，是修一個真 bug —— 原本 `apply_registry_scan` 每次成功掃描
   都 `self.error.take()`，1 秒的輪詢會在使用者讀到之前把送出失敗的訊息清掉。
   測試會綠只是因為測試控制時鐘。顯示位置與機制沒換，保留。

3. **空白訊息被擋掉：接受。**
   誤殺的合法輸入是「只按 Enter」—— 在 Claude Code 裡那等於接受權限提示的
   預設選項。擋掉是對的：輸入框有焦點時誤按 Enter 會直接授權一個可能破壞性的工具。
   使用者要接受提示仍可打「1」再 Enter，或按 Esc 中斷。
   若日後要支援，應該做成明確的「Approve」按鈕，不是讓空訊息穿過去。

4. **三支 registry 測試改了呼叫方式：接受，我逐字比對過。**
   斷言文字的雜湊在兩支上完全相同；第三支
   （`test_registry_scan_normalizes_padded_process_start`）唯一差別是
   `session.process_start` 變成 `summary.session.process_start`，語意不變。
   WP6 說「padded 那支現在有一半在驗測試 helper」是對的，但它仍然釘住
   store 的列舉路徑吃 padded procStart 會判 Live；等價的產品路徑覆蓋在
   `remote` 自己的 `test_list_sessions_reports_sessions_with_and_without_a_transcript`
   與 `test_normalize_whitespace_matches_padded_ps_output`。兩邊都留。

5. **`transcript_path()` 最多落後一次 registry 輪詢（1 秒）：接受。**
   「還沒有 transcript」與「transcript 是空的」兩句提示的切換慢一拍，不是 bug。

另外 WP6 回報 `./script/clippy` 全 workspace 編不過（`trusted_worktrees.rs:161` E0004）
—— 我實測 `./script/clippy` exit code 0，涵蓋 claude_sessions / remote / remote_server 三個
crate 都有 Checking。它那次應該是在樹還在半改狀態時跑的。不採信該回報。

--------------------------------------------------------------------------------
E.10 WP9（RemoteSource）交付與它自己回報的缺口
--------------------------------------------------------------------------------

交付：`RemoteSource` 四支方法對接四支 RPC（`project_id` 一律
`proto::REMOTE_SERVER_PROJECT_ID`）；panel 依 `project.read(cx).remote_client()`
選 source；`session_summary_from_proto` / `tail_progress_from_proto` 抽成純函式加測試。
147 支測試（140 → 147），盲測 87 支未動。

`process_start` 在 remote 填空字串、`kind` 填 `"interactive"`、`bridge_session_id` 填 None：
判活與 interactive 過濾已經在 process 所在的那台機器做完，client 手上沒有那台機器的
pid table，再判一次只會更差。

**送出失敗還回文字**（E.9 第 1 項）的機制選了「store 的 `send_input` 回傳
`Task<Result<()>>`」而不是事件 —— panel 需要「這次的結果」與「這次的文字」配對，
回傳 task 讓兩者留在同一個 stack frame，不會錯配。界限用 `is_empty()` 而非
`trim().is_empty()`：輸入框裡只要有一個字元就是使用者的，不做「那只是空白」的判斷。
三支測試各用一次突變證明紅在對的原因（拿掉還原邏輯 / 拿掉「仍為空」守衛 / 不看 outcome）。

它回報的四件事，我的處置：

  1. **remote 上 persisted output 的按鈕永遠不會出現** —— 兩處守衛用的是
     `paths::home_dir()`（本機 home），而 remote transcript 裡是遠端路徑。
     它照指令沒有放寬界限，回報。**這是真缺口 → 開 WP10 修**：
     `ListClaudeSessionsResponse` 加 `home_directory`，client 用「產生那些路徑的那台機器」
     的 home 做界限判斷；home 還沒拿到時按鈕不顯示（不得拿本機 home 頂著用）。
  2. **`NO_SESSIONS` 在遠端說謊**（"on this machine"）→ 併進 WP10 改掉。
     `READ_ONLY_NOTE` 與 `NO_PANE_TARGET` 是 session 層級敘述，不動。
  3. **`send_input` 的 task 被丟掉會把 tmux 寫入砍到一半** —— 目前兩個呼叫端
     一個 await、一個 detach，走不到；已寫在 doc comment，型別無法強制。
     留給 Wave 4 review 判斷是否值得換成 channel（要新增依賴，不擅自加是對的）。
  4. `process_start` 在 remote 是空字串：panel／store 都不讀它，判活不在 client 做。接受。

--------------------------------------------------------------------------------
E.11 跨家族安全審查（Gemini，唯讀）的 findings
--------------------------------------------------------------------------------

這批 P3/P4 的程式碼主要是 Opus 寫的，所以安全面交給不同家族審（CLAUDE.md 要求）。
唯讀、只找不修、只准輸出 JSON。它審五個界限，回報 A/C/E 乾淨、兩條 finding：

**S1（high）—— `validate_claude_file_path` 界限過寬。根因是我 spec 寫錯。**

  它允許整個 `<home>/.claude/`，於是 client 可以用 `proto::ReadClaudeFile` 讀走
  `<home>/.claude/sessions/<pid>.<sha256>.key` —— 正文 §1.1／§8 明文
  「不讀、不記、不傳」的那個檔。

  這條界限是我在 E.4 的派工裡寫成「必須落在 `<home>/.claude/` 之下」的，
  WP7 照做沒有錯。實際上被 offload 的 tool 輸出只住在
  `<home>/.claude/projects/<slug>/tool-results/*.txt`，而 client 端同一條界限
  （`persisted_output_is_loadable`）用的就是 `home/.claude/projects` ——
  兩邊本來就不一致，寬的那邊在 server。

  這正是「安全／金流／刪除加第二輪、換不同家族」這條規則要抓的東西：
  同家族審同家族不會有人去質疑派工裡那行界限。

**S2（medium）—— `send_text` 沒有失敗／取消的清理。**

  `load-buffer` 成功之後若 `paste-buffer` 失敗（pane 不存在）或 Task 被 drop，
  那個 buffer 會留在 tmux server 的 buffer stack 頂端，使用者手動貼上
  （prefix + `]`）會意外貼出剛才那段文字。若 drop 發生在 paste 之後、
  `send-keys Enter` 之前，文字會留在終端機裡沒送出。

兩條都交給 Wave 4 的 Opus review 那一輪獨立驗證、寫 RED 測試、修到綠
（來源標 handed，誤報要標 rejected 並說明理由）。

--------------------------------------------------------------------------------
E.12 Review 輪（Opus，乾淨 context）結果與大腦驗收
--------------------------------------------------------------------------------

findings 6 = 修 3 + wontfix 3。對帳平衡：RED 測試 3 支 ↔ 修正 3 條 ↔ `src/` 改動 3 處
（我比對 review 前的快照，只有 `remote/src/claude_sessions.rs` 與
`remote_server/src/headless_project.rs` 兩檔被動過，其餘四檔 byte-identical）。

**修掉的三條**

  S1（high，handed）server 端界限由 `<home>/.claude` 收到 `<home>/.claude/projects`。
  S2（medium，handed）`paste-buffer`/`send-keys` 失敗時補 `delete-buffer`，
    不遮蓋原始錯誤。取消（Task 被 drop）造成的殘留無法在 await point 清理，回報保留。
  **R1（high，這輪自己找的）—— 比 S1 更嚴重的同類漏洞**：
    `find_transcript` 把 `format!("{session_id}.jsonl")` 直接 join 到 projects 底下，
    而 `session_id` 從來沒有被當成不可信輸入驗過。我在 E.8 只擋了 client 傳來的 `path`，
    沒想到真正組出路徑的是 `session_id`。
      concrete input：`TailClaudeTranscript { session_id: "../../../../../../etc/ssl/private/secrets" }`
      後果：任何 remote server 讀得到的 `*.jsonl` 都能被讀走。
    修法：新增私有 `transcript_file_name(session_id)`，`{session_id}.jsonl` 必須是
    **恰好一個 `Component::Normal` 且等於自身**。放在共用層，所以四支 RPC、
    `list_sessions`、`LocalSource` 一次全關。

**wontfix 三條**：R2（併發 send 交錯，見 E.13）、R3（`display_working_directory`
在 remote 用本機 home 縮寫路徑，純顯示且 fail open）、R4（`overwritten_uuids`
無上限成長，P1 範圍、每筆約 40 bytes）。

**我的驗收（不採信回報）**

  gate：claude_sessions 151、remote 94、remote_server 56 全綠；clippy exit 0；
  fmt clean；zed 與 remote_server build 綠；盲測三檔去空白雜湊未變；
  收工時 tmux 沒有 `zed-claude-*` buffer 殘留。

  三處修正各做一次突變，全部紅在對的斷言：
    `transcript_file_name(session_id)?` → 改回 `format!(...)`
      → `a_session_id_cannot_lead_the_transcript_search_out_of_the_projects_directory` 紅，
        訊息帶出實際解析到的越界路徑。
    `delete-buffer` 清理拿掉
      → `a_send_that_cannot_reach_its_pane_leaves_no_paste_buffer_behind` 紅，
        訊息顯示 tmux 仍持有 `zed-claude-89580-0`（我事後確認殘留已清掉，現在是 0）。
    `.join("projects")` 改回 `.claude`
      → `a_read_is_confined_to_the_directory_the_client_may_offer` 紅。

  誤殺驗證：真的 7 個 session 過新的 `transcript_file_name` 之後，
  仍然是 6 個解析到 transcript（與加界限之前相同），0 誤殺。

**程序違規（review agent 自首）**

  它在一次 Bash 呼叫裡把 `sed -n '1,120p' blind_input_tests.rs | head -60` 跟 keymap 的
  diff 綁在同一個指令，讀到了盲測檔前 ~60 行。我的判定：
  盲測三檔的雜湊未變（未被修改）、它的三處修正都不在 `pane_target`
  （diff 證實：改的是 `transcript_file_name`、`delete-buffer`、server 界限），
  所以那 16 支 `pane_target` 盲測**仍然是有效證據**。但這件事記在這裡：
  日後若要改 `pane_target`，對這一隻而言那些測試已經不算盲測。

--------------------------------------------------------------------------------
E.13 R2 從 wontfix 改成修（大腦解除了它的阻礙）
--------------------------------------------------------------------------------

review 輪把 R2（兩個併發 send 的三個 tmux 行程會交錯，導致兩則訊息被併成一則、
再送一個空行）標成 wontfix，理由是「要驗證得對真 pane 送輸入，這輪禁止」。

那個阻礙是我的規則造成的，所以我自己去驗：開一個拋棄式 tmux session
（`cat >> /tmp/probe.out`），實測**一次呼叫**的 command list 可行：

  printf 'line one\nline two' | tmux load-buffer -b probe1 - ';' \
    paste-buffer -d -p -b probe1 -t %11 ';' send-keys -t %11 Enter
  → exit 0、檔案收到 "line one\nline two\n"（多行保留、Enter 送出）、buffer 不殘留

tmux server 是單執行緒事件迴圈、逐個處理一個 client 的 command list，
所以一次呼叫就沒有中間可被別的 send 插進來的空隙。已派工改成這個形式，
並要求：失敗清理照舊保留（load-buffer 成功後 paste 失敗時 buffer 仍會留下）、
`send_escape` 不動、端到端驗證只能用拋棄式 session、收工不得留下 session 或 buffer。

**規格補充（R1 交回的規格漏洞，一併定案）**：
`sessionId` 一律視為不可信輸入 —— 它只能構成**單一 path component**，
client、server、local 三邊用同一條規則。

--------------------------------------------------------------------------------
E.14 R2 修完 + 大腦把 opt-in 測試改成預設跑
--------------------------------------------------------------------------------

`send_text` 收成**一次** tmux 呼叫：

  load-buffer -b <name> - ";" paste-buffer -d -p -b <name> -t <pane> ";" send-keys -t <pane> Enter

`";"` 是獨立的 argv 元素，不經 shell；文字仍然只走 stdin。抽出純函式
`send_text_arguments(buffer_name, pane_target)` 釘住「單一 argv、兩個 `;`、順序正確」——
併發競爭本身無法穩定重現，所以釘的是結構性質：任何人把它拆回三個呼叫就會紅。
失敗清理（`delete-buffer`）保留，因為 command list 裡 paste 失敗時 load 已經成功、buffer 會留下。
`send_escape` 未動。

**大腦補的一件事**：它把端到端那支（真的開一個 `cat` session、送兩行、讀檔驗內容）
設成環境變數 opt-in，理由是會跟既有的
`a_send_that_cannot_reach_its_pane_leaves_no_paste_buffer_behind` 互踩
（後者斷言「此 process 沒有 `zed-claude-<pid>-` 前綴的 buffer」，前者會短暫持有一個）。
它不准改既有測試，所以只能 opt-in —— 代價是那支不在預設路徑上，等於 CI 不跑。

我把根因解掉：`mod tests` 加一個 `TMUX_SERVER_TESTS: Mutex<()>`，兩支都先取鎖
（poison 用 `into_inner()` 跨過，免得一支失敗連帶讓另一支噴出誤導的第二個失敗），
然後移除環境變數 gate。既有測試只多一行取鎖，**斷言一字未改**。

  連跑三次 `cargo test -p remote` → 96 passed / 0 failed，無 flake。
  非空測證明：把 command list 最後的 `Enter` 改成 `Space`
    → `a_multi_line_send_arrives_in_the_pane_as_one_message` 紅在
      left: "line one\n" / right: "line one\nline two\n"。
  收工時 tmux 沒有 `zed-claude-*` session 或 buffer 殘留。

它自己列的殘留風險，我的處置：
  - 「兩則訊息的先後順序」不保證（panel 兩個 detach task 誰先跑到 tmux 誰先送）：
    接受。使用者連按兩次 Enter 的兩則訊息都會完整送到，只是順序可能互換，
    不會被併成一則，也不會misroute 到別的 session。
  - 「錯誤訊息變粗」（整串 command list + tmux stderr）：接受，仍看得到 tmux 的原因。
  - 「依賴 tmux 對 `;` 的語意」：本機 3.6b 實測成功與失敗路徑都符合；
    很舊的 tmux 會退化成什麼，純函式測試抓不到，只有端到端那支抓得到 ——
    而它現在預設會跑。

--------------------------------------------------------------------------------
E.15 P3 + P4 交付狀態
--------------------------------------------------------------------------------

  測試：claude_sessions 151（含 87 支盲測）、remote 96、remote_server 56
  gate：./script/clippy exit 0、cargo fmt --check clean、
        cargo build -p zed 與 -p remote_server 都 0 error
  新程式碼零 `#[allow]` / `#[expect]`、非測試碼零 `unwrap()` / `expect()`
  README 前兩行完好

  實機驗證（本機 7 個真 session）：
    列舉 7/7 判 Live、transcript 找到 6/7（第 7 個是 SDK session，確認檔案不存在）
    增量 tail：6 個 session 第一次讀完、第二次回 0 行且 restarted false
    路徑界限：6 個真 transcript 全部通過 `transcript_path_to_follow`，誤殺 0
    session id 界限：真 UUID 仍解析到 transcript（6/7，與加界限前相同）

  **P3 的實際送出沒有由 agent 驗過**（規則明文禁止對真 session 送輸入）。
  端到端只驗到「送進一個跑 `cat` 的拋棄式 pane，兩行合成一則、Enter 送出」。
  真正打進一個活著的 Claude session 要由使用者自己按一次。

  仍然欠的：
    - WP2 / WP3（P1 的 transcript.rs / session_store.rs）欠一輪跨家族 review，
      grok 額度用盡（見 C.4）。P3/P4 這一輪已經補上跨家族安全審查（E.11）。
    - panel 的接線層沒有 entity 級測試（要 Workspace harness），
      這一輪 review 評估後標 wontfix。
    - R3（remote 上 `display_working_directory` 用本機 home 縮寫）、
      R4（`overwritten_uuids` 無上限）留在 backlog。


================================================================================
附錄 F P2 任務書（2026-09-10 訂，含實機量測）
================================================================================

--------------------------------------------------------------------------------
F.0 正文 §4 有一個假設是錯的
--------------------------------------------------------------------------------

§4 最後一句寫「Workflow 的 `agent()` 產出同樣落在 `subagents/`，所以不需要為
Workflow 另做一套；同一個機制涵蓋」。**實機量測否證了這句。** 有兩種佈局：

  A. `Agent` tool 起的：`<sessionId>/subagents/agent-<agentId>.{jsonl,meta.json}`
     meta 有 `toolUseId` → 可以直接對回主對話的 tool_use block。（本機 116 個）
  B. Workflow 起的：`<sessionId>/subagents/workflows/<runId>/agent-<agentId>.{jsonl,meta.json}`
     **meta 沒有 `toolUseId`**，改帶 `workflowPhase`；同目錄另有 `journal.jsonl`。
     （本機 278 個）

  路徑深度只有這兩種（`subagents/` 之後 1 段或 3 段，實測 116 / 278，沒有更深的）。

  B 的連結方式：`Workflow` 的 **tool_result 內文含 `Run ID: wf_xxx`**，
  用那個 runId 對回目錄名。實測：`wf_b529a29d-562` 的 tool_result 的
  `tool_use_id` 是 `toolu_01PmRN5V7q41k1VXakxdF3A9`。

--------------------------------------------------------------------------------
F.1 事實清單（全部實機量測，不要再猜）
--------------------------------------------------------------------------------

meta.json 的完整欄位（兩個真實樣本）：

  A 型：{"agentType":"general-purpose","description":"Phase B adversarial review round 2",
         "toolUseId":"toolu_01BEgVRSnksoz6YAyUWEEdeQ","spawnDepth":1,
         "requestShape":"background","requestNonInteractive":true,"model":"opus"}
  B 型：{"agentType":"workflow-subagent","description":"R2:send-atomicity",
         "workflowPhase":"Wave 5","spawnDepth":1,"requestShape":"foreground",
         "requestNonInteractive":false,"model":"opus"}

  欄位可缺席，實測分佈（全機器 394 個 meta）：
    242  spawnDepth=1  workflow-subagent  無 toolUseId  無 workflowPhase
    100  spawnDepth=1  general-purpose    有 toolUseId
     28  spawnDepth=1  workflow-subagent  無 toolUseId  有 workflowPhase
      8  spawnDepth=1  general-purpose    **無 toolUseId**   ← 不能假設 A 型一定有
      4  spawnDepth=2  general-purpose    有 toolUseId
      3  spawnDepth=3  general-purpose    有 toolUseId
      其餘：fork / Explore / claude 等 agentType
  → `toolUseId` 一律 `Option`，`description` / `model` / `workflowPhase` 也是。
  → `agentType` 是自由字串（`general-purpose` / `workflow-subagent` / `fork` /
    `Explore` / `claude` / 使用者自定），不得做成 enum。

subagent 的 jsonl 記錄形狀**與主 transcript 完全相同**（實測 keys）：
  agentId, cwd, entrypoint, gitBranch, isSidechain, message, parentUuid,
  promptId, sessionId, timestamp, type, userType, uuid, version
  - `isSidechain: true`、`sessionId` 是**母 session 的 id**、多了 `agentId`。
  - type 分佈（102 行的樣本）：assistant 41 / attachment 36 / user 25。
  → **`transcript.rs` 的 `parse_record` 與樹走訪可以原封不動重用**，這是 P2 便宜的原因。

  檔案大小實測：一隻 workflow agent 的 jsonl 有 519 KB（102 行的那隻是小的），
  所以一律走既有的增量 tail，不要整檔重讀。

`spawnDepth >= 2` 的 `toolUseId` 指向的是**母 agent 的** tool_use，
但實測那個 id 在主 transcript 檔裡也找得到（母 agent 的記錄以 sidechain 形式
同時寫進主檔）。所以連結時掃「主檔的所有記錄（含 sidechain）」就夠，
不需要另外去翻 agent jsonl。

`journal.jsonl`（只有 B 型有）每行一筆，實測 type 有 `launched` / `started` / `result`，
`started` 帶 {key, agentId, label, phase}，`result` 帶 {key, agentId, result}。
→ B 型的「跑完了沒」可以直接看有沒有該 agentId 的 `result` 行，比 mtime 準。

`pendingBackgroundAgentCount` **不能當即時徽章**：它掛在
`{"type":"system","subtype":"turn_duration",...}` 記錄上，是那一輪結束時的統計值
（實測本機只出現 2 次、值都是 1）。即時數量要自己算：
有 meta 但主對話還沒出現對應 tool_result 的算 running。

--------------------------------------------------------------------------------
F.2 WP11 —— 資料層（Opus 5 high）
--------------------------------------------------------------------------------

全部加在 `crates/remote/src/claude_sessions.rs`（共用層，remote server 也要用）：

  pub struct SubagentMeta {
      pub agent_type: String,          // "agentType"
      pub description: Option<String>,
      pub tool_use_id: Option<String>, // "toolUseId"
      pub spawn_depth: u32,            // "spawnDepth"，缺席視為 1
      pub model: Option<String>,
      pub workflow_phase: Option<String>, // "workflowPhase"
  }
  /// 未知欄位忽略（不要 deny_unknown_fields），缺 agentType 回 Err。
  pub fn parse_subagent_meta(contents: &str) -> anyhow::Result<SubagentMeta>

  pub struct SubagentSummary {
      pub agent_id: String,
      pub workflow_run_id: Option<String>,   // B 型才有
      pub meta: SubagentMeta,
      pub transcript_path: PathBuf,
      pub size: u64,                          // 用來當「有沒有在動」的提示
  }
  /// 掃 <home>/.claude/projects/*/<sessionId>/subagents/ 的兩種佈局。
  /// 單一 meta 壞掉只跳過該筆，不得整批失敗。
  pub async fn list_subagents(home_directory: &Path, session_id: &str) -> anyhow::Result<Vec<SubagentSummary>>

  /// agent transcript 的路徑。**三個 id 都是不可信輸入**（來自 JSON 與 RPC），
  /// 每一個都必須是單一 path component，否則回 None（規則與 E.13 定的一致）。
  pub fn subagent_transcript_path(
      home_directory: &Path, session_id: &str, agent_id: &str,
      workflow_run_id: Option<&str>,
  ) -> Option<PathBuf>

  /// Workflow 的 tool_result 內文裡的 `Run ID: wf_xxx`。純函式、要能被測。
  pub fn workflow_run_id_in_tool_result(text: &str) -> Option<String>

硬性要求：
  - `session_id` / `agent_id` / `workflow_run_id` 三者都過單一 component 檢查，
    共用同一個 helper，不要各寫一份。
  - 不得讀 `~/.claude/sessions/` 底下任何東西。
  - 排序：先 B 型的 runId、再 agentId，穩定且不得依賴目錄列舉順序。

--------------------------------------------------------------------------------
F.3 WP12 —— 盲測（Opus 5 medium，禁看實作）
--------------------------------------------------------------------------------

只給 F.2 的 API 與 F.1 的事實，**不准讀** `crates/remote/src/claude_sessions.rs`
與 `crates/claude_sessions/src/`。寫成
`crates/claude_sessions/src/blind_subagent_tests.rs`（大腦負責掛 mod）。
覆蓋：三個 id 的淨化（`..`、`/`、空字串、`%`、超長）、meta 解析的欄位缺席與未知欄位、
`workflow_run_id_in_tool_result` 的正反例。

--------------------------------------------------------------------------------
F.4 之後幾波（順序，細節等前一波落地再定）
--------------------------------------------------------------------------------

  WP13  source trait 加 `list_subagents` / `tail_subagent` + LocalSource（Opus）
  WP14  proto + remote_server handler（Gemini，樣板同 E.4）
        —— `TailClaudeTranscript` 加 `optional agent_id` / `optional workflow_run_id`，
           或另開一支；server 端三個 id 都要過同一條淨化。
  WP15  panel UI（Opus high）：頂端 agent chip 列（label 用 description、
        跑著的加 `●`、結束的灰掉但留著）＋主對話裡 inline 可展開的 agent 卡片，
        兩個入口都要有；切到某隻 agent 時用**同一條 render pipeline**。唯讀。
  WP16  review 輪（Opus 乾淨 context）＋ 跨家族安全審查（Gemini，只審新的路徑界限）

--------------------------------------------------------------------------------
E.16 一個我下錯的結論，與它真正的原因
--------------------------------------------------------------------------------

**我先寫下的（錯的）結論**：使用者用 dev build 送了幾則訊息之後，回報他的
Claude Code terminal「打字畫面上看不到，但 Claude 收得到」。我判定是
`paste-buffer -p` 把 TUI 留在 bracketed paste 狀態，理由是他手動送
`ESC [ 2 0 1 ~` 之後 terminal 恢復了。我在這份文件裡寫成「已確認的證據」。

**推翻它的觀察**（使用者自己發現）：顯示不出來的那兩則訊息，內容都正好是
`<task-notification>…</task-notification>` 那一串。**Claude Code 的 TUI 自己就會
把這種系統標籤的文字藏起來**，所以字打得進去、模型收得到、畫面上不顯示。
恢復不是結束標記的功勞 —— 那之後他打的是普通文字，本來就會顯示。

**我錯在哪**：拿「做了 X 之後好了」當成「X 是原因」。正確的驗證應該是
請他在同一個 pane 再打一次那串標籤（會再次不顯示），或打一段普通文字
（會顯示）—— 兩者都不需要動任何程式碼就能分辨。我沒做這一步就寫進文件。

**對 P3 的影響**：目前**沒有任何證據**顯示送出功能弄壞過使用者的終端機。

**U5 的理由改寫**（施工內容不變）：只有多行才用 `-p` —— 不是因為它修了一個
已確認的 bug，而是因為 `-p` 的作用只在多行（讓多行不被逐行送出），
單行送出沒有理由讓終端機進入一個它不需要的模式。這條站得住腳，但它是
「不要做沒必要的事」，不是「修好了一個災難」。

**取消**原本計畫的「多行送出時額外補一個結束標記」：那是建立在錯誤診斷上的，
沒有證據支持，而且會往使用者的終端機多送一段沒必要的控制序列。

**對 U1 的補強**：這件事反過來證實 U1 的方向是對的 —— TUI 自己都藏這些區塊，
panel 沒有理由把它們當成使用者訊息顯示。

--------------------------------------------------------------------------------
E.17 WP17 —— 「它現在在做什麼」的即時指示（排在 UX 那一輪之後）
--------------------------------------------------------------------------------

使用者要求：thinking 的時候要有動畫，才知道 Claude 有沒有在動。

**現況**：`render_session_row` 只有一顆靜態的 `IconName::Indicator`，
綠色的條件是 `session.status == Some("busy")`。而 `status` 來自註冊檔，
**只在狀態改變時才被改寫**（附錄 D.2 量過：idle 的 session 可以 3336 秒不動），
所以那顆點既不會動、也不保證準。

**不要用計時器或 `updatedAt` 來判斷「在動」**，用 transcript 自己的內容 ——
它是唯一真的跟著模型前進的東西，而且純函式可測、不需要時鐘：

  enum Activity { Thinking, RunningTool { name, target }, Idle }
  /// 由 active path 的尾端推導。純函式、無 IO、無時鐘。
  fn activity(path: &[&TranscriptRecord]) -> Activity

  判定規則（依序）：
    1. 尾端是 assistant 記錄且含 `thinking` block、其後沒有 user／tool_result 記錄
       → Thinking
    2. 尾端有 `tool_use` 且主對話裡還沒有對應 `tool_use_id` 的 tool_result
       → RunningTool { name: 那個 tool 的 name,
                        target: 從 input 取一段短摘要（Bash 取 command 前 ~40 字、
                                Read/Edit/Write 取 file_path 的檔名、其他留空）}
    3. 其餘 → Idle

**UI**
  - session row 的那顆點：`status == "busy"` 時用既有的 `with_animation` 慣用法
    脈動（樣板：`crates/agent_ui/src/agent_panel.rs:6092`，
    `Animation::new(Duration::from_secs(1)).repeat().with_easing(pulsating_between(0.2, 0.6))`，
    closure 收 `|icon, delta| icon.opacity(delta)`）。不得新增 asset。
  - 選中的 session 在對話區下緣（輸入框上方）多一行狀態：
      Thinking            → 「Thinking…」+ 脈動
      RunningTool         → 「Running Bash: cargo test -p …」+ 脈動
      Idle                → 不顯示這一行（不要留空位跳動）
  - 這一行是**唯讀指示**，不得有任何按鈕、不得觸發送出。

**測試**：`activity` 的三條規則各兩支（正例 + 邊界），用既有的
`entries_of` 那種以 JSONL 字串建構的寫法；動畫本身不測（GPUI 動畫沒有可斷言的輸出），
但要有一支測試釘住「Idle 時不產生那一行」。

--------------------------------------------------------------------------------
E.18 順序（使用者已回報 subagent / workflow 沒顯示 —— 那就是 P2，還沒做）
--------------------------------------------------------------------------------

  現在跑著：UX 輪（U1-U5，動 panel 與 remote/claude_sessions.rs）
  接著並行：WP11 P2 資料層（只動 remote/claude_sessions.rs）
            WP17 即時指示（只動 panel）
  然後：    WP13/WP14（source trait + proto + server）
  最後：    WP15 P2 的 panel UI（agent chip 列 + inline 卡片）—— 必須排在 WP17 之後，
            兩者動同一個檔。
  收尾：    WP16 review 輪（Opus 乾淨 context）+ 跨家族安全審查（Gemini）

--------------------------------------------------------------------------------
E.19 WP18 —— 送出的訊息在 transcript 出現之前要先顯示
--------------------------------------------------------------------------------

使用者回報：「我打字的時候，如果 claude 還沒處理，panel 不會顯示。」

這是架構的必然，要分兩種情況講清楚：

**(a) 在 terminal 裡打字** —— panel 讀的是 transcript 檔，而 CLI 只在開始處理那一輪
時才把 user 記錄寫進檔案。所以使用者在終端機裡打了一半、或打完還沒送出的字，
**panel 在任何情況下都看不到**。要看到只有兩條路，兩條都不做：
  - `messagingSocketPath` 的本機 IPC（正文 §6.2：協定未文件化，第一版不碰）
  - `tmux capture-pane -p -t <pane>` 把 pane 畫面抓下來解析輸入框
    （tmux 官方功能，但要解析 TUI 畫的框線，版本一變就壞）
  → 這一項的正確處置是**在 UI 上講明白**，不是硬做。

**(b) 從 panel 送出** —— 這個可以修，而且該修。現在送出之後輸入框清空，
但要等 CLI 把記錄寫進 transcript（可能好幾秒，busy 的 session 更久）才看得到，
中間使用者不知道自己送出去了沒有。

要做：
  - 送出時立刻在對話尾端插入一筆 **pending** entry（顯示送出的文字 +
    「Sending…」或脈動，樣式上明顯不同於已送達的訊息）。
  - 真正的 user 記錄從 transcript 出現時移除 pending：比對條件是
    **trim 之後文字相同、且是送出之後才新增的 user 記錄**（第一個相符的就配對）。
  - 送出失敗（已有的還原路徑，E.9 第 1 項）→ 移除 pending，文字回到輸入框。
  - 切換 session → 清掉 pending（它屬於原本那個 session）。
  - pending 不進 entry cache、不影響 transcript 的樹與 offset，
    也不得讓 `activity()`（E.17）誤判成 Thinking。
  - **不得**做自動重送、不得用計時器判斷「應該出現了」而把 pending 靜默丟掉：
    配不上就一直顯示成 pending，那才是誠實的。

測試：送出後 pending 出現；相同文字的 user 記錄到達後 pending 消失且不重複顯示；
文字不同的 user 記錄到達不會誤配；送出失敗時 pending 消失；切換 session 時 pending 消失。

  **使用者實測補充（提高優先度）**：從 panel 送出後，terminal 那邊看得到
  （文字已經在 TUI 的輸入框／排隊中），但 panel 看不到，**感覺像字消失了**。
  這比原本寫的「不知道送出去了沒」嚴重：它讓人不信任這個工具。
  → WP18 提到下一波的第一件事，與 WP17 併成同一輪（都只動 panel）。

  兩個補充規則：
    - 同樣的文字連送兩次 → 兩筆 pending，與後續到達的兩筆 user 記錄**依序配對**（FIFO）。
    - pending 配不上時不用計時器丟掉（那會再一次讓字消失），改成
      **pending 泡泡上給一個手動關掉的 ×**，由使用者決定它是否已經沒意義。

--------------------------------------------------------------------------------
E.20 U1 的追加：slash command 的三個標籤（使用者回報）
--------------------------------------------------------------------------------

實機量測（本機 session transcript）：slash command 的呼叫是 `type:"user"`、
content 為純字串的記錄，內容形如

  <command-name>/goal</command-name>
            <command-message>goal</command-message>
            <command-args>幫我把 claude session panel 都修好 新需求做完</command-args>

各標籤出現次數：`<command-name>` 12、`<command-message` 10、`<command-args>` 7、
`<local-command-stdout>` 17、`<local-command-caveat>` 2。

**處置不是「整筆藏掉」**：`<command-args>` 裡是使用者真正打的字，藏掉會讓對話斷掉
（例如這份文件所有的 `/goal` 指示都會消失）。規則：

  <command-name>X</command-name> + <command-args>Y</command-args>
    → 顯示成一行 slash command：`X Y`（X 已經含 `/`），樣式與一般訊息可區分
      （例如 Muted 的等寬字），args 為空就只顯示 X
  <command-message>…</command-message>   → 移除（純內部欄位）
  <local-command-stdout>…</local-command-stdout> → 移除（指令自己的輸出，
      例如 "Goal set: …"、`/context` 的整張表）
  <local-command-caveat>…</local-command-caveat>  → 移除

這一條併進下一波（WP17/WP18 那一輪）施工，因為 U1 的那支
`user_visible_text` 已經在這一輪落地，追加標籤只是擴充它的清單 + 一條新的顯示規則。
測試：`/goal` 帶 args、`/compact` 不帶 args、只有 `<local-command-stdout>` 的記錄
（整筆不顯示）、args 內含 `<` 之類字元不得破壞剝離。

--------------------------------------------------------------------------------
F.5 規格補洞：subagent 路徑的 <slug> 從哪來（盲測作者提出）
--------------------------------------------------------------------------------

F.2 把 `subagent_transcript_path` 寫成純函式，但路徑裡有一段 `<slug>`
（`<home>/.claude/projects/<slug>/<sessionId>/subagents/...`），而簽名沒有 slug 參數。
盲測作者在不看實作的情況下指出這一點，並把測試寫成能同時容納
「純函式推導 slug」與「掃目錄找 slug」兩種實作 —— 這是正確的處理。

**定案**：跟 P1 的 `find_transcript` 完全一樣的規則 ——
**不得自己把 cwd 的 `/` 換成 `-` 去組 slug**（slug 規則未文件化，正文 §2 已經定過），
一律 glob `<home>/.claude/projects/*/<sessionId>/`（把 sessionId 當成**目錄**名）
找到 session 目錄，再往下接 `subagents/...`。

因此 `subagent_transcript_path` **會做目錄 IO**，與 `find_transcript` 同一個層級，
不是純函式。淨化（三個 id 各為單一 path component）仍然抽成獨立的純函式共用，
那一支才是盲測釘的核心。找不到 session 目錄 → None。

--------------------------------------------------------------------------------
E.21 WP19 —— 排版與顏色向網頁版對齊（使用者附了截圖）
--------------------------------------------------------------------------------

使用者：「顏色有點不夠好看、不夠多彩，網頁版的排版顏色都弄得更好。」

**先釘清楚現況，不要重做已經對的東西**：panel 的訊息文字已經走
`MarkdownStyle::themed(MarkdownFont::Agent, window, cx)`（claude_sessions_panel.rs
的 `markdown_style`），而且 `Markdown::new(source, language_registry, None, cx)`
有傳 language registry —— 所以**訊息裡的 markdown 與 fenced code block 本來就有語法高亮**。
不夠好看的是它周圍的東西。

截圖裡網頁版做到、我們沒做到的（逐項對照）：

  1. **工具呼叫的指令本身沒有語法高亮**：截圖裡 `! cd /Users/... && npx ts-node
     scripts/migrate-credit-denomination.ts --apply` 是一個有顏色的 shell 程式碼區塊。
     我們現在把 tool_use 的 input 當成純文字顯示。
     → 改成把 input 包成 fenced code block 交給 Markdown 渲染：
       Bash → ```bash（取 `command`）、Read/Edit/Write → 依 `file_path` 的副檔名選語言、
       其他工具 → ```json（整個 input pretty print）。語言選擇要抽成純函式加測試。
  2. **工具輸出沒有框**：截圖裡輸出是一個獨立的圓角邊框卡片，
     `(no output)` 是 Muted 斜體。我們現在是接在同一段文字裡。
     → tool_result 用 `border_1` + `rounded_sm` + `bg(colors.editor_background)` 的卡片，
       空輸出顯示 `(no output)`（Muted）。
  3. **長輸出沒有高度上限**：截圖裡長輸出有固定最大高度 + 右下角展開的 chevron。
     → 超過約 12 行就夾住並給一個展開/收起的 disclosure（既有的 `render_disclosure_header`
       已經有這個機制，沿用它，不要另做一套）。
  4. **inline code 沒有底色**：截圖裡 `.claude/settings.json`、`getMyQuota`、`X-POC-Model`
     都有淡色底。`MarkdownStyle.inline_code` 是現成欄位，themed 版本應該已經有；
     如果實際看起來沒有，就在 `markdown_style` 裡覆寫它（用 theme 的
     `colors().element_background` 之類，**不得寫死顏色**）。
  5. **角色沒有顏色區分**：assistant / user / summary / 工具 四種在截圖裡是不同的視覺層次。
     → 每種角色的 icon 與標籤用 theme 既有語意色（`Color::Accent` / `Color::Muted` /
       `Color::Success` / `Color::Error`），**一律走 theme，不得出現任何 rgb/hex 常數**。

硬性要求：
  - **不得寫死任何顏色**：全部從 `cx.theme().colors()` / `Color::*` 取，
    否則換 theme（含淺色）就爛掉。
  - 不得新增 asset 檔、不得引入新的 icon 檔；IconName 只用既有的。
  - 不得改動 transcript 解析、offset、界限判斷等非顯示邏輯。
  - 既有測試（包含四個盲測檔）不得改。
  - 顏色與框線是視覺，沒辦法斷言；**能測的部分一定要測**：
    語言選擇的純函式（Bash → bash、`.rs` → rust、`.py` → python、未知副檔名 → 空、
    其他工具 → json）、以及「輸出超過 N 行才夾住」的判定。

--------------------------------------------------------------------------------
E.22 UX 輪的驗收與大腦裁定
--------------------------------------------------------------------------------

交付：163 / 98 / 56 全綠（claude_sessions +12、remote +2），clippy exit 0、fmt clean、
zed build 綠；另外因為動到共用的 settings crate，加跑 `cargo test -p settings`（37）
與 `-p settings_ui`（58），都綠。盲測三檔去空白雜湊未變。

我的突變抽查：
  `user_visible_text` 直接回傳原文（等於沒剝除）→ 5 支測試變紅
  `needs_bracketed_paste` 改成永遠 true（單行也 `-p`）→ 1 支變紅
兩處都已還原成 byte-identical。

它回報的六件事，裁定如下：

1. **「沒有結尾標籤」採「完全不刪」**：接受，而且這是比我原本寫的更好的讀法。
   我寫「不得把後面全部吃掉：只到文字結尾為止」有歧義；照另一種讀法會讓既有測試
   `a_persisted_output_marker_in_message_text_stays_a_message`（fixture 是無結尾標籤的
   `<persisted-output>`）變成 0 個 entry 而 index panic，而它不准改既有測試。
   選「畸形輸入完全不刪」的理由對：**寧可多顯示，不可弄丟使用者的字**。
   實測五種包裝在真實 user 記錄裡都是成對的，所以實務無差。
2. U5 既有測試只改呼叫方式（多一個 `true` 參數）、17 行期望 argv 一字未動：接受。
3. U5 端到端測不出 `-p`（`cat` 沒開 bracketed paste 模式，tmux 就不發那組序列）：
   接受這個限制，`-p` 由 argv 測試釘住。它為了不動既有測試而付出約 40 行 plumbing 重複，
   這個取捨是對的。
4. **U1 的誤殺面**：使用者若在訊息裡成對寫出這些標籤（例如討論這個功能本身），
   那一段會被隱藏。維持現狀，不加「標籤必須獨占整行」的條件 ——
   加了會讓貼進來的通知反而不被隱藏（那正是使用者要藏的東西）。記錄在此。
5. **「N new messages」與計數單位不一致**：它照規格做但同時指出矛盾，是對的。
   **我自己改掉了**：文案改成「N new below」，並在該處註解說明為什麼不說 messages
   （一次回覆會是 thinking + tool_use + tool_result + text 好幾個 entry，
   說 messages 會給出一個讀者對不上的數字）。163 支維持綠、clippy 0。
6. `blind_subagent_tests.rs` 還沒掛進 crate root：正確，那是大腦的事，
   要等 WP11 的 API 落地才掛（現在掛會編不過）。

**它自己抓到並修掉的一個 GPUI 陷阱**（`discoveredWhileFixing`）值得記著：
`ListState::set_scroll_handler` 的 `event.is_following_tail` 只有在
`FollowState::Tail{is_following:true}` 才為 true，而這個 panel 從未設 `FollowMode::Tail`
→ 永遠 false，計數永遠不會被清掉；而且 handler 在 `StateInner::scroll` 的
`borrow_mut()` 裡跑，在裡面呼叫 `is_scrolled_to_end()` 會 RefCell double-borrow panic。
它改成在 render 時判斷，並且只在 `== Some(true)` 時清空 ——
`None`（splice 後新 item 還沒量高度）當成 true 會把剛到達的那筆直接抹掉。

--------------------------------------------------------------------------------
F.6 WP11 驗收與裁定
--------------------------------------------------------------------------------

交付：`remote` 109 支（claude_sessions 模組 36 → 47），claude_sessions 163、
remote_server 56 全綠，clippy exit 0、fmt clean、zed build 綠。
突變證明貼了真實的逃逸路徑（把 `single_path_component` 短路之後，
session_id `../..`、agent_id 帶分隔符、run_id `..` 三條都真的踩到既存檔案）。
誤殺檢查：真實 UUID／`af090e203ec41bc73`／`wf_b529a29d-562` 三種形狀都解析到正確路徑。

額外的實機交叉驗證（它自己做的，值得記）：掃過本機**全部 402 個** meta
（120 flat / 282 workflow，深度只有 1 段與 3 段兩種，flat 層沒有非 `agent-*` 的檔）：
`agentType` 402/402 是 string、`spawnDepth` 出現時全是 number（1×393、2×6、3×3）、
四個 optional 欄位沒有任何一筆是 `null`。所以 `parse_subagent_meta` 對現存全部真實檔案都會成功。

**它改了一行既有 code，我覆核後接受**：`transcript_file_name` 的 body 改成
先過 `single_path_component`。行為差異只有變嚴（session_id 為 `.` / `..` 從
「回一個找不到檔的名字」變成直接 None），既有測試未改仍綠。
同一個檔留兩份同類界限是更差的選擇，接受。

四個裁決：

1. **`subagent_transcript_path` 檔案不存在回 None**（它加了 `is_file()`）：**維持**。
   與同檔 `find_transcript` 同語意。agent 的 transcript 還沒寫出來時，
   `list_subagents` 仍會列出那隻（`size = 0`，有測試釘住），UI 顯示「還沒有 transcript」
   —— 與主 session 的處理一致。
2. **`workflow_run_id_in_tool_result` 第一個被界限擋掉時會找下一個**（「第一個**合法**的」）：
   **維持**。兩種讀法都安全（擋掉的那個本來就不會被用），它這個讀法比較有用。
3. **`list_subagents` 對 NotFound 以外的 read_dir 失敗回 Err**：**維持**。
   照 `read_registrations` 的既有慣例。一個不可讀的 workflows 目錄讓整批失敗，
   比靜默少列幾隻 agent 好 —— 使用者看得到錯誤訊息。
4. **`list_subagents` 是 async 但體內沒有 await**：那是我指定的簽名，維持。
   呼叫端一律放到 background executor（與 `list_sessions` 相同），
   workflows 目錄多的 session 會有 N+1 次 read_dir —— 記在此，之後量到慢再說。

--------------------------------------------------------------------------------
F.7 派工瑕疵：兩個範圍之間有一個沒人擁有的檔案
--------------------------------------------------------------------------------

WP14 在 `TailClaudeTranscript` 加了 `agent_id` / `workflow_run_id` 兩個欄位之後，
唯一的 client 端呼叫點 `crates/claude_sessions/src/session_source.rs:141` 的
struct literal 少欄位，`crates/claude_sessions` 整包編不過：

  error[E0063]: missing fields `agent_id` and `workflow_run_id` in initializer
                of `rpc::proto::TailClaudeTranscript`

而 WP14 的範圍是 `crates/proto` + `crates/remote_server`、同時跑的 panel 那一輪範圍是
`claude_sessions_panel.rs` + `session_store.rs` —— **`session_source.rs` 不在任何人的範圍內，
沒有人能修**。這是我切範圍時的疏漏：我按「檔案」切，但沒有檢查改動的 blast radius
會不會落在切口上。

panel 那一輪的 agent 主動回報阻塞、並且**沒有擅自擴大範圍**（它補了那兩行以便跑自己的 gate，
同時明說收工前會還原、gate 是在 shim 之下取得的）。那是正確的處理方式。

處置：那兩行是正式的（主對話的 tail 本來就不指定 agent），我接管
`session_source.rs`，並回覆該 agent 不要還原。之後 WP13 會把 None 換成真正的 subagent 定址。

**規則補充**：改 proto 訊息時，派工要一併點名「所有 struct literal 的呼叫點」，
或明確指定一個人擁有那些呼叫點。用 codegraph 查該訊息的 blast radius 再切範圍。

--------------------------------------------------------------------------------
F.8 WP13 驗收與兩個必須先裁決的發現
--------------------------------------------------------------------------------

交付：claude_sessions 212、remote 112（+3）、remote_server 59 全綠，clippy exit 0、fmt clean。
它證實並修掉了我在派工時點出的 fall back 缺陷，RED 是真的：

  a tail of subagent af090e203ec41bc73 whose transcript is gone must report no lines,
  but it reported ["{\"type\":\"user\",\"uuid\":\"main-only\"}"], which is the main
  conversation from Some(".../-some-slug/4e2e3600-….jsonl")

修法：`read_subagent_transcript_tail` 每次自己用三個 id 解析路徑、**沒有任何 fall back**，
解析不到就回 path None / lines 空。handler 的 subagent 分支改呼叫它，主對話那條一字未動。

**發現 1（它抓到的，比 fall back 更重要）：subagent 的對話會畫成空白。**
  一份真的 agent transcript **496 行全部 `isSidechain: true`**（我自己用 jq 獨立驗過）。
  而 transcript.rs 的兩處守衛（第 135 行 absorb 的 leaf、第 226 行 walk 的 path）
  刻意排除 sidechain —— 那對主對話是對的（subagent 的記錄也會以 sidechain 形式寫進主檔），
  但當 transcript 本身就是一份 subagent 對話時等於把全部內容濾掉。
  它沒有動 transcript.rs（不在範圍、且被 39 支盲測蓋著），選擇回報。正確。

  **裁定**：給 `Transcript` 一個「這份 transcript 本身就是 sidechain 對話」的模式
  （`Transcript::for_sidechain()`，內部一個 bool，兩處守衛變成
  `if self.reads_sidechain || !record.is_sidechain`）。預設行為一個字不變，
  所以 39 支盲測與 18 支既有測試維持綠。store 在 target 是 Subagent 時建這種 transcript。

**發現 2（我推導出來的）：chip 的狀態需要主對話，但切到 agent 之後主對話已經被換掉。**
  「跑著沒」要看主對話有沒有對應的 tool_result，而 store 只有一份 transcript。
  **裁定**：store 同時維持兩份 —— 主對話**永遠**在追（它也是偵測 `/clear` 的依據），
  另外在有選中 agent 時追那隻。`transcript()` 回現在看的那一份、
  新增 `main_transcript()` 給狀態判定用，`transcript_generation()` 與 `transcript_path()`
  都要跟著現在看的那一份走。

其餘四點裁定：
  - `transcript_path()` 看 subagent 時仍回主路徑 → 併進 WP15 修（見上）。
  - `headless_project.rs` 的 `tail_target` subagent 分支現在 production 走不到，
    只剩兩支既有測試在測它 → 留給收尾 commit 一起清掉（動它會改到既有測試斷言）。
  - 協定行為變更（agent_id 解析不到從 RPC error 變成空 TailProgress）：**接受**，
    這讓 remote 與 local 對稱（LocalSource 不可能 error）。
  - `select_transcript_target(Subagent)` 在沒選 session 時留下懸空 target：
    UI 走不到（chip 來自 `subagents()`，沒選中就是空的），接受。

--------------------------------------------------------------------------------
F.9 跨家族安全審查：第一次回答被退回，第二次過關
--------------------------------------------------------------------------------

**第一次**回了 83 個位元組：
  {"findings":[],"checked_and_clean":["A","B","C","D","E"],"could_not_determine":[]}
五個界限（含 symlink、Windows 分隔符、generation 撞號）全部宣告乾淨，拿不出任何根據。
同一隻上一輪做同樣任務時回了 2116 位元組並抓到一個 high（E.11 的 S1）。
**我退回它**，並在重派的 prompt 加一個必填欄位 `evidence_per_boundary`：
實際讀過的行號區間、界限為什麼擋得住（要引用實際判定）、考慮過的輸入類別、
以及**「拿掉這個檢查會被什麼輸入打穿」** —— 說不出後者就必須放進 `could_not_determine`，
不准放 checked_and_clean。

**第二次**回了 7055 位元組，五個界限各有行號與機制，findings 仍是 0。
我抽驗三條，全部對上：

  A  `single_path_component`（claude_sessions.rs:285-295）守衛是
     `only_component == OsStr::new(name)` —— 與它引述的一致。
     它舉的打穿輸入：拿掉檢查後 `session_id = "../../sessions"` 會讓
     `directory_entry.path().join(session_id)` 走出 projects 並命中 `~/.claude/sessions`。
  C  store 建 `Transcript::new()` 在 158 / 612 / 679，`for_sidechain()` 只在
     239（`select_transcript_target` 的 Subagent 分支）與 680（restart 時依 target 選）。
     **主對話沒有任何路徑會拿到 sidechain 模式** —— 這是這個設計最怕的事，實地確認過。
  E  `announced_workflow_run_id` 真的有 `.filter(|(id, _)| *id == tool_use_id)`，
     所以隨便一個 Bash 輸出印 `Run ID: wf_x` 劫不走卡片 —— 它只讀同一個 tool_use 自己的 result。

**流程教訓**：「零 finding」是可以接受的結果，但必須附得出可查核的證據。
沒有證據的「全部乾淨」與沒審過沒有差別 —— 而且這一次證明了差別在哪：
同一個模型、同一份程式碼、同一個任務，第一次 83 位元組、第二次 7055 位元組。
往後所有唯讀審查的 prompt 都要帶 `evidence_per_boundary` 這個欄位。

--------------------------------------------------------------------------------
F.10 Review 輪 + 我駁回它三條 wontfix 的結果
--------------------------------------------------------------------------------

**Review 輪（Opus，乾淨 context）**：10 findings = 修 3 + wontfix 6 + rejected 1（它自己的誤報）。
修掉的是 R1（pending 與送出**之前**就存在的同文字記錄配對）、R4（記錄層級的
`toolUseResult` 被套到該筆的每一個 tool_result block）、R5（已離開的 session 的讀取失敗
報在現在這個 session 上）。R4 它給 low 是有根據的：掃過 875 個真 transcript、
8315 筆答工具的 user 記錄，**零筆**有超過一個 tool_result block。
它也獨立重驗那五個界限，結論與 Gemini 一致。

**我駁回它三條 wontfix**（R2 / R3 / R6），理由是它的前提錯了 ——
它說「沒有 Workspace harness 就寫不出 RED，抽出來的純函式會從出生就是綠的」。
正確做法是抽一個把「該用哪一份 transcript」變成可斷言的 seam，再**用突變證明它接在對的地方**。

派工後它自己建了 entity harness（`ScriptedSource` + `scripted_panel`：
`WeakEntity::new_invalid()` + `FakeFs` + 一個 `NoUi` root window，不 render 以避開
`GlobalThemeSettingsProvider`，並照 `new` 接上同一條 `cx.observe`）—— **不需要新 dev-dep**，
所以「值不值得建」的答案是值得。這也回頭補上了 P1 就留著的 G5（沒有 entity 級測試）那個缺口。

三條的實際缺陷：
  R3（high）活動指示從**螢幕上那一份**推導。看一隻已跑完的 agent 時，
     它的 transcript 尾端常是一個沒有 tool_result 的 tool_use → 那行**永遠脈動 Running…**，
     而輸入框是停用的；反過來 session 真的在 thinking 時看 agent 的人看不到。
  R2（high）pending 泡泡被畫進 agent 的對話，且配對拿 agent 的記錄比 ——
     agent 的第一筆 user 記錄就是它被交付的任務，文字若與送出的訊息相同就會**誤配掉**，
     真正沒送達的訊息再也不顯示。
  R6（medium）`render_disclosure_header` 上的 `#[allow(clippy::too_many_arguments)]`。

**它誠實回報的一件事，我覆核為真**：那個 `#[allow]` **從來沒有作用** ——
根 `Cargo.toml:1142` 有 `too_many_arguments = "allow"`（我實地確認過），
所以刪掉前後 clippy 都是 exit 0，R6 的「紅」只能是 grep 命中。它照樣做了 struct 重構。

**「全樹零 `#[allow]`」這條它做不到也不該做**：`crates/` 底下有 604 處、散在 211 個檔案，
全部是 upstream zed 的。規則的範圍是我們寫的程式碼 —— `crates/claude_sessions/` 與
`crates/remote/src/claude_sessions.rs` 現在是 **0**（我獨立 grep 確認）。

**我的突變抽查**（各自還原成 byte-identical）：
  `activity(&session_path)` → `activity(&path)`            → 2 支紅
  `pending_is_drawn_in` 的 Subagent 分支 false → true      → 1 支紅

**它交回給我裁決的灰色地帶，裁定如下**：`agent_calls(&path)` 仍從螢幕上那一份推導 ——
看某隻 agent 的對話時，那份對話裡的 `Agent`/`Workflow` 呼叫會拿 `subagents()` 去配對。
**維持現狀，這是對的**：實機量測過 spawn_depth 2/3 的 agent 也是平放在同一個
`subagents/` 目錄（附錄 F.1 的分佈表：4 筆 depth 2、3 筆 depth 3），
所以它們本來就在 `subagents()` 裡；而巢狀 agent 的 `toolUseId` 指向的正是**母 agent
transcript 裡的那個 tool_use** —— 於是看母 agent 時用 tool_use_id 配對剛好配到對的那隻。
